import PracticeArcade from '@/components/games/PracticeArcade';

export default function SyntaxSpeedPage() {
  return (
    <PracticeArcade
      mode="syntax-speed"
      title="Syntax Recall Circuit"
      description="Build accurate language recall without a timer penalty. Type a visible target, then reproduce the pattern with changed names and values."
    />
  );
}
