import PracticeArcade from '@/components/games/PracticeArcade';

export default function CodeBuilderPage() {
  return (
    <PracticeArcade
      mode="code-builder"
      title="System Builder"
      description="Assemble production workflows in causal order, then adapt the sequence when the operating conditions change."
    />
  );
}
