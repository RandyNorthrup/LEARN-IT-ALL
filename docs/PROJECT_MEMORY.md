# LEARN-IT-ALL Project Memory

Last verified full gate: 2026-07-15 after the independent product studios and institution-grade capstone release wave

This is the canonical repository-local handoff. A fact under **Verified rebuild state** passed the recorded full gate. Anything described as planned or active has not. Generated files and large counts are not completion evidence.

## Mission

This repository is undergoing a total platform and curriculum rebuild, not a cosmetic cleanup or a small extension of the old format. The target is a modern, accessible, responsive, fun, local-first, highly interactive platform dominated by practice, debugging, and authentic building. Course order must be prerequisite-driven, cumulative, complete from entry skills through independent mastery, and duplication-audited.

## Non-negotiable user decisions

- Use `/caveman` for agent communication.
- Keep active goal and plan updated.
- Add or update tests with new code and content.
- Make all UI/UX accessible and responsive.
- Suggest next steps and explain blockers at each session handoff.
- Preserve unrelated work in the intentionally dirty worktree.
- Report `planned`, `generated`, `schema-valid`, `flow-verified`, and `published` as different states.
- Upgrade stack to latest mutually compatible working releases, then repair fallout.
- Responsive Web Design must cover the depth and topics of freeCodeCamp Responsive Web Design v9 with original LEARN-IT-ALL material.
- Every course, including preexisting courses, needs theory, workshops, labs, debugging, reviews, quizzes, real-world projects, exams, and repeated build-to-learn practice.
- Each lesson must retrieve and correctly reuse earlier skills before adding bounded new complexity. Generated quantity and renamed variants do not establish quality.
- Lighthouse is paused until all content work is complete. Final Lighthouse runs must cover mobile, tablet, and desktop, exclude SEO, and reach at least 99 in performance, accessibility, and best practices.

## Current architecture

- Next.js App Router application with React, TypeScript, SQLite, and version 2 curriculum documents.
- Repository and container toolchain: Node.js 24.18.0 LTS and npm 12.0.1, pinned in package metadata, engines, the lockfile, Docker, and tested lifecycle-script policy.
- Canonical curriculum types and audits: `src/core/curriculum`.
- Learner-facing version 2 content: `content/v2/courses/<course-id>`.
- Course blueprints: `blueprints/<course-id>.json`.
- Shared content generators: `scripts/lib/generate-course-blueprint.mjs` and `scripts/lib/generate-expanded-course-content.mjs`.
- Shared interactive targets include sandboxed web output, JavaScript, Pyodide Python, isolated Go WebAssembly, fresh in-memory SQLite, deterministic network/shell simulation, prompt evaluation, repository/config quality gates, and numeric calculation.
- Legacy content is migration input. Do not extend old repeated Markdown/JSON layouts as the target model.

## Verified rebuild state

At the last full gate, 53 courses were wired into the version 2 learner path and published in the catalog:

- Responsive Web Design: 29 modules, 303 activities, 4,855 steps, and 5,407 checks.
- Python Basics, Python OOP, and CompTIA Network+ N10-009: 45 modules, 529 activities, 4,588 steps, and 5,027 checks combined.
- Beginner, Intermediate, and Advanced Applied Mathematics; Prompt Engineering with Claude and Codex; Agent Loops and Goals; Creating Agent Skills and MCP Servers; and Repository Quality Gates: 103 modules, 1,065 activities, 8,768 steps, and 9,348 checks combined.
- Modern JavaScript and Modern TypeScript 7: 29 modules, 314 activities, 2,615 steps, and 2,871 checks combined.
- Python Functional Programming, Python Data Structures and Algorithms, and Advanced Algorithm Design in Python: 44 modules, 455 activities, 3,912 steps, and 4,279 checks combined.
- Linux Foundations and Git Foundations: 28 modules, 290 activities, 2,511 steps, and 2,745 checks combined.
- SQL and Relational Data Systems: 16 modules, 165 activities, 1,342 steps, and 1,475 checks.
- Modern Go 1.26: 18 modules, 186 activities, 1,514 steps, and 1,664 checks.
- HTTP Clients in Go, TypeScript, and Python: 54 modules, 558 activities, 4,542 steps, and 4,992 checks combined.
- Modern Docker 29: 18 modules, 186 activities, 1,514 steps, and 1,664 checks.
- Modern Kubernetes 1.36: 21 modules, 216 activities, 1,757 steps, and 1,931 checks.
- Secure CI/CD with GitHub Actions, TypeScript, Docker, and Cloud Run: 20 modules, 206 activities, 1,676 steps, and 1,842 checks.
- Advanced Git 2.55: 20 modules, 206 activities, 1,678 steps, and 1,844 checks.
- HTTP Servers in Go 1.26: 21 modules, 216 activities, 1,762 steps, and 1,936 checks.
- HTTP Servers in TypeScript: 24 modules, 246 activities, 2,006 steps, and 2,204 checks.
- HTTP Protocol Engineering in Go 1.26: 24 modules, 246 activities, 2,006 steps, and 2,204 checks.
- RabbitMQ 4.3 Pub/Sub and Reliable Messaging with TypeScript 7: 27 modules, 276 activities, 2,257 steps, and 2,479 checks.
- RabbitMQ 4.3 Reliable Messaging with Go 1.26: 27 modules, 276 activities, 2,249 steps, and 2,471 checks.
- Production File Delivery with Amazon S3 and CloudFront in Go 1.26: 28 modules, 286 activities, 2,332 steps, and 2,562 checks.
- Production File Delivery with Amazon S3 and CloudFront in TypeScript 7: 28 modules, 286 activities, 2,332 steps, and 2,562 checks.
- Memory Management and Memory Safety in Modern C23: 28 modules, 286 activities, 2,333 steps, and 2,563 checks.
- Applied Cryptographic Engineering in Go 1.26: 28 modules, 286 activities, 2,331 steps, and 2,561 checks.
- Retrieval-Augmented Generation Engineering with Python 3.14: 30 modules, 306 activities, 2,494 steps, and 2,740 checks.
- Build and Ship a Unicode-Safe Bookbot with Python 3.14: 18 modules, 186 activities, 1,519 steps, and 1,669 checks.
- Build and Ship Accessible Asteroids with Python 3.14 and pygame-ce 2.5.7: 24 modules, 246 activities, 2,006 steps, and 2,204 checks.
- Build and Ship a Production Static Site Generator with Python 3.14: 24 modules, 246 activities, 2,006 steps, and 2,204 checks.
- Build and Ship an Accessible Maze Solver with Python 3.14 and Tkinter: 24 modules, 246 activities, 2,006 steps, and 2,204 checks.
- Build and Ship an Authorized Web Crawler and Accessible Site Auditor with Python 3.14: 30 modules, 306 activities, 2,494 steps, and 2,740 checks.
- Build and Ship a Safe Evaluated AI Agent with Python 3.14 and Gemini 3.5: 30 modules, 306 activities, 2,494 steps, and 2,740 checks.
- Build and Ship a Production-Grade Pokedex in Go 1.26: 24 modules, 246 activities, 2,006 steps, and 2,204 checks.
- Build and Ship a Production-Grade Pokedex in TypeScript 7 and Node 24: 25 modules, 256 activities, 2,087 steps, and 2,293 checks.
- Build and Ship a Production Feed Aggregator in Go 1.26: 30 modules, 306 activities, 2,494 steps, and 2,740 checks.
- Build and Ship a Production Feed Aggregator in TypeScript 7 and Node 24: 32 modules, 326 activities, 2,656 steps, and 2,918 checks.
- Build and Ship an Authorized Web Crawler and Accessible Site Auditor in Go 1.26: 30 modules, 306 activities, 2,494 steps, and 2,740 checks.
- Build and Ship an Authorized Web Crawler and Accessible Site Auditor in TypeScript 7 and Node 24: 32 modules, 326 activities, 2,656 steps, and 2,918 checks.
- Independent Product Studio I: 12 modules, 126 activities, 1,031 steps, and 1,133 checks.
- Independent Product Studio II: 14 modules, 146 activities, 1,194 steps, and 1,312 checks.
- Institution-Grade Computing Capstone: 16 modules, 166 activities, 1,356 steps, and 1,490 checks.
- Evidence-Driven Programming Career Launch: 20 modules, 206 activities, 1,681 steps, and 1,847 checks.

Verified platform total at this checkpoint: 1,095 modules, 11,335 activities, 95,554 learner steps, and 104,727 checks across 53 version 2 courses.

The catalog contains 54 courses total. At this checkpoint, current CompTIA A+ is the only catalog course not yet a verified version 2 release and still requires current-objective research, blueprinting, robust original content, runtime integration, catalog migration, subject review, and learner-flow verification.

## Evidence-driven programming career launch release evidence

Evidence-Driven Programming Career Launch has 20 prerequisite-ordered modules, 100 distinct observable competencies, 206 activities, 1,681 learner steps, 1,847 checks, and five cumulative career projects across about 303 guided hours. It requires the Institution-Grade Computing Capstone. The order advances from learner-owned outcomes, constraints, and sustainable search decisions through competency evidence, bounded labor-market research, role decomposition, smallest-evidence gap plans, public-identity safety, GitHub proof, case studies, resumes, tailored application packages, measurable search systems, scam resistance, relationship building, informational interviews, screening, technical and collaborative evidence, behavioral and access decisions, offer evaluation, negotiation, transition, resilience, and a verified first-90-day plan. Each module retrieves prior evidence and preserves truthfulness, accessibility, privacy, consent, security, learner agency, changed-case verification, failure analysis, and defensible transfer.

Research was reviewed 2026-07-15 against NACE's current eight career-readiness competencies and bounded 2025 skills-based-hiring poll, 2024 BLS data and 2024-2034 projections, current O*NET occupation and 2025 posting-demand data, U.S. Department of Labor CareerOneStop search and salary guidance, current GitHub profile and repository-health documentation, EEOC applicant-disability and employment-AI resources, the FTC's 2025 job-scam alert, the ACM Code of Ethics, and WCAG 2.2 with its ISO/IEC 40500:2025 status. Location, date, sample, and jurisdiction limits are explicit. External sources control coverage and evidence depth only; all explanations, scenarios, assessments, artifacts, and projects are original.

The new `career` configuration workspace performs deterministic text-only review of target role, jurisdiction, audience, bounded claim, revision-linked evidence, process state, accessibility, privacy and consent, learner decision authority, verification, rejected failure with causal repair, and changed-case transfer. The provided placeholder starter scores 1/12; complete fictional evidence can score 12/12; omitting the repair after a rejected failure scores 11/12. No application, contact, publication, account, employer, email, external site, or host command is reached from the learner simulator.

Course-specific and platform tests enforce exact prerequisite and module order, all eight activity kinds, complete introduce/guided/faded/retrieve/assess/transfer coverage, unique concepts and cases, distinct projects, source records, catalog publication, safe simulator routing, interactive step depth, no hidden-answer leakage, and cross-course concept duplication checks. Live catalog, course, and full first-activity verification passed at 390x844 mobile, 768x1024 tablet, and 1440x900 desktop with one H1, no document overflow, 44-pixel controls, reduced motion, visible 3-pixel keyboard focus, and no application console or page errors. The live run completed prediction, theory, inspection, ordering, career evidence review, debugging, and reflection; a valid changed artifact scored 12/12. Browser review caught and fixed a duplicated `evidence evidence` phrase and desktop code-diff wrapping that had reduced evidence text to one-character fragments. Lighthouse remained intentionally paused.

## Independent product studios and capstone release evidence

Independent Product Studio I has 12 prerequisite-ordered modules, 60 competencies, 126 activities, 1,031 learner steps, 1,133 checks, and five distinct product milestones across about 199.8 guided hours. Studio II has 14 modules, 70 competencies, 146 activities, 1,194 steps, 1,312 checks, and five advanced milestones across about 225.6 hours. The Institution-Grade Computing Capstone has 16 modules, 80 competencies, 166 activities, 1,356 steps, 1,490 checks, and five gated defenses across about 251.4 hours. Together they add 210 distinct competency statements, 438 activities, 3,581 learner steps, 3,935 checks, and 15 non-duplicated stakeholder project needs.

Studio I advances from prior-skill retrieval and evidence baselining through problem-first discovery, ethical inclusive research, outcome and guardrail contracts, riskiest-assumption prototypes, vertical slices, reproducible repository stewardship, bounded architecture and data invariants, cumulative core behavior, responsive accessible human evaluation, security/privacy/reliability evidence, release identity, clean recovery, documentation, and independent claim defense. Studio II begins with real maintenance evidence, then adds multi-actor service discovery, co-design and equity, auditable workflow state, quality-attribute architecture, governed data migrations, identity and accessible account recovery, tenant isolation and abuse resistance, idempotency and partial failure, collaboration governance, user-centered service levels and incident learning, capacity and cost, supply-chain provenance, progressive delivery, migration, clean restore, longitudinal validation, and personal versus team evidence. The capstone gates mastery claims, independence disclosure, significant public-value evidence, ethics and harms, authoritative domain and licensing boundaries, outcome evaluation, feasibility and exercised stop decisions, evidence-ordered scope, architecture review, domain/API/algorithm invariants, threat and privacy engineering, cumulative construction, integration resilience, layered automated and human evaluation, calibrated claims, capacity and operations, incident game day, trusted release, isolated restore, retirement, public documentation, unseen defense, and reflective transfer.

Research was reviewed 2026-07-15 against ACM/IEEE/AAAI CS2023, the ACM Code of Ethics, NACE's current eight career-readiness competencies, WCAG 2.2 and its ISO/IEC 40500:2025 status, NIST SSDF 1.1 final, NIST Privacy Framework 1.0 final while explicitly identifying 1.1 as draft, current GitHub project-presentation, repository-health, and security documentation, GOV.UK discovery and alpha practices, SLSA 1.2, and SPDX 3.0.1. External sources control coverage and evidence depth only; all learner-facing scenarios, explanations, assessments, and projects are original.

The new `portfolio` configuration workspace reviews stakeholder and decision evidence, problem counterevidence, accessibility and human review, security and abuse, privacy lifecycle, changed-case testing, release identity and provenance, clean recovery, contribution and unseen defense, deterministic verification, rejected failure and causal repair, and controlled transfer. Its browser simulator analyzes text only. Explicit bracketed starters score 1/12 until learners replace them with concrete evidence; a live browser audit caught and repaired a whitespace-regex false positive that had credited placeholder guidance. Visual review also caught and removed a generic Go-language rationale leak from all 3,581 product-course steps.

Course-specific and cross-platform tests enforce schema and prerequisite order, all eight activity kinds, stage coverage, unique competency/module/project concepts, unique stakeholder needs, authoritative source versions, safe configuration-only browser workspaces, no generic Go leakage, exact counts, catalog publication, and initial placeholder rejection. A full live first-activity path completed prediction, theory, keyboard ordering, code evidence, and configuration review. Catalog, all three course routes, and the activity route passed one-H1, zero-overflow, 44-pixel button, reduced-motion, visible 3-pixel keyboard-focus, console, and page-error checks at 390x844 mobile, 768x1024 tablet, and 1440x900 desktop. Visual screenshots passed review. Lighthouse remained intentionally paused.

## Authorized Go and TypeScript crawler release evidence

Build and Ship an Authorized Web Crawler and Accessible Site Auditor in Go 1.26 has 30 prerequisite-ordered modules, 150 competencies, 306 activities, 2,494 learner steps, 2,740 checks, 246 exact compile-ready Go evidence builds, and five cumulative stakeholder projects. Build and Ship an Authorized Web Crawler and Accessible Site Auditor in TypeScript 7 and Node 24 has 32 prerequisite-ordered modules, 160 competencies, 326 activities, 2,656 learner steps, 2,918 checks, 262 exact TypeScript evidence builds, and five distinct cumulative projects. Both require their language, HTTP-client, Responsive Web Design, Git, and quality-gate foundations. They progress from owner authorization and stakeholder outcomes through URL identity, RFC 9309 robots policy, sitemaps, bounded HTTP and decoding, HTML parsing, typed extraction and provenance, semantic link graphs, deterministic frontiers, trap control, origin fairness, bounded concurrency, checkpoints, deduplication and change, metadata, accessibility and performance evidence, browser-service boundaries, sessions and privacy, untrusted-content security, accessible reports, fault testing, operations, recovery, and release defense. Go separately teaches package and value ownership, `net/url`, streaming XML, `net/http`, `DialContext` SSRF controls, `x/net/html`, goquery, goroutine/context workers, Colly, `httptest`, fuzzing, and race evidence. TypeScript separately teaches source-to-emission contracts, `unknown` runtime admission, WHATWG URL and Fetch streams, Cheerio/parse5, `p-limit`, Promise and AbortSignal ownership, Playwright, axe-core, `node:test`, negative types, package emission, and browser-binary operations.

Research was reviewed 2026-07-15 against RFC 9309, the Sitemap protocol, WHATWG URL and HTML Living Standards, HTTP RFC 9110 and 9111, WCAG 2.2, current OWASP SSRF and XSS guidance, Go 1.26.5, goquery 1.12.0, Colly 2.3.0, robotstxt 1.1.2, x/net 0.57.0, Node 24.18.0, TypeScript 7.0.2 and the TypeScript 6 compatibility compiler, Cheerio 1.2.0, robots-parser 3.0.1, p-limit 7.3.0, csv-stringify 6.8.1, Playwright 1.61.1, and axe-core 4.12.1. Disposable native smokes compiled and ran the exact Go stack, exercised the exact Node packages over loopback, and launched Playwright's installed Chromium 149.0.7827.55 at 768x1024 with reduced motion and zero axe violations. Browser lessons remain deterministic pure-language models; real DNS, sockets, parsers, files, processes, browser services, race, load, accessibility review, legal review, restore, and production behavior remain explicit controlled transfer gates.

Course-specific gates enforce exact prerequisite order, all eight activity kinds, introduce/guided/faded/retrieve/assess/transfer coverage, distinct projects and scenarios, unique evidence contracts, source authority and version records, language separation, and browser purity. Every Go learner block parses and every exact Go evidence function compiles. Every TypeScript block strict-compiles and every exact default function runs under both compiler paths. Cross-blueprint exact and near-duplication audits pass. Both first activities passed all seven prediction, theory, inspection, keyboard ordering, exact build, debugging, and causal-reflection layers; Go ran in the isolated Go worker and TypeScript passed production compiler diagnostics. Course and activity routes passed one-H1, no-overflow, 44-pixel target, visible 3-pixel keyboard-focus, overlay, console, and page-error checks at 390x844 mobile, 768x1024 tablet, and 1440x900 desktop. Twelve route/profile checks and visual screenshot review passed.

## Production feed-aggregator release evidence

Build and Ship a Production Feed Aggregator in Go 1.26 has 30 prerequisite-ordered modules, 150 competencies, 306 activities, 2,494 learner steps, 2,740 checks, 246 exact compile-ready Go evidence builds, and five cumulative stakeholder projects. Build and Ship a Production Feed Aggregator in TypeScript 7 and Node 24 has 32 prerequisite-ordered modules, 160 competencies, 326 activities, 2,656 learner steps, 2,918 checks, 262 exact TypeScript evidence builds, and five separate cumulative projects. Both begin only after their language, HTTP-client, HTTP-server, SQL, Git, quality-gate, and Docker foundations. Coverage progresses from reader and publisher outcomes through authorized discovery and SSRF boundaries, RSS 2.0 and Atom 1.0, bounded XML, loss-aware normalization, conditional HTTP, PostgreSQL constraints and migrations, transaction ownership, shared sources and subscriptions, versioned entries, atomic ingestion, scheduling, fenced leases, bounded workers, resource APIs, tenant privacy, cursor timelines, search, accessible content-safe reading, personal state, caching, observability, tests and faults, security and retention, deployment, backup, restore, recovery, and production defense. The TypeScript track separately teaches unknown-value validation, Fetch streams, AbortSignal and Promise ownership, Express layers, AsyncLocalStorage, handle cleanup, package emission, and runtime contracts rather than translating Go mechanics.

Research was reviewed 2026-07-15 against RSS 2.0.11, Atom RFC 4287, XML 1.0, URI RFC 3986, WHATWG URL and feed discovery, HTTP RFC 9110 and 9111, Go 1.26.5, Node 24.18.0, TypeScript 7.0.2 and the TypeScript 6 compatibility surface, pgx 5.10.0, goose 3.27.2, Express 5.2.1, pg 8.22.0, fast-xml-parser 5.10.0, Zod 4.4.3, Pino 10.3.1, PostgreSQL 18.4, OpenAPI 3.2, RFC 9457, current OWASP SSRF and XSS guidance, WCAG 2.2, NIST SSDF, OpenTelemetry conventions, and ACM/IEEE/AAAI CS2023. Disposable native smokes imported and exercised the exact Node packages, compiled pgxpool and goose with Go 1.26.5, and ran the official PostgreSQL 18.4 container image. Browser examples stay deterministic and pure; real network, XML-library, database, process, load, container, accessibility-review, backup, restore, and production claims remain controlled transfer gates.

Course-specific gates require exact module order, prerequisites, introduce/guided/faded/retrieve/assess/transfer coverage, eight activity kinds, distinct workshop/debug/lab/review/quiz and project contexts, one evidence profile per module, unique changed-case builds, source authority/version records, language separation, browser purity, Go parsing and compilation, and strict TypeScript compilation plus default execution on both compiler paths. Both complete first activities passed all seven prediction, theory, inspection, keyboard ordering, exact build, debugging, and causal-reflection layers. Go also ran in the isolated Go 1.26.5 browser worker; TypeScript passed the production server compiler with no diagnostics.

Live production verification exposed a standalone packaging defect: Next.js traced the TypeScript compiler JavaScript but omitted its standard-library declaration files, so the browser compiler could not find global types after deployment. `outputFileTracingIncludes` now ships all `@typescript/old` library declarations for the runtime route, a regression test fixes the contract, the rebuilt standalone artifact contains 110 declaration files including `lib.es2024.full.d.ts`, and the same learner source then passed strict diagnostics. Course and studio routes passed one-H1, no-overflow, 44-pixel visible-target, 3-pixel focus, reduced-motion, overlay, console, and page-error checks at 1440x900 desktop, 768x1024 tablet, and 390x844 mobile.

## Production Pokedex release evidence

Build and Ship a Production-Grade Pokedex in Go 1.26 has 24 prerequisite-ordered modules, 120 competencies, 246 activities, 2,006 learner steps, 2,204 checks, 198 exact runnable Go evidence builds, and five cumulative stakeholder projects. Build and Ship a Production-Grade Pokedex in TypeScript 7 and Node 24 has 25 prerequisite-ordered modules, 125 competencies, 256 activities, 2,087 learner steps, 2,293 checks, 206 exact TypeScript evidence builds, and five cumulative projects. Both start after their language and HTTP-client foundations, Git Foundations, and Repository Quality Gates. They progress from learner outcomes, command vocabulary, REPL/session lifecycles, fixed-origin PokéAPI access, unknown JSON validation, bounded HTTP behavior, pagination, history, caching, duplicate-request suppression, detail and catch commands, owned collections, location exploration, errors, cancellation, concurrency, persistence, security, testing, observability, fair use, packaging, recovery, and release. The TypeScript track adds a dedicated Node readline and stream-lifecycle module instead of translating Go mechanics.

Research was reviewed 2026-07-15 against PokéAPI v2 documentation and its fair-use cache guidance; Go 1.26.5 release, context, signal, `net/http`, and `httptest` documentation; Node 24.18 globals, Fetch, AbortSignal, readline/promises, streams, and test-runner documentation; TypeScript 7.0.2 and the TypeScript 6.0.2 compatibility surface; WCAG 2.2; current packaging, security, testing, and professional curriculum authorities. Browser builds use deterministic pure-language models and original fixed fixtures only. Real PokéAPI traffic, DNS, sockets, TLS, host files, terminal streams, signals, race/leak/load claims, package inspection, representative-platform checks, accessibility review, recovery, and production behavior remain explicit controlled transfer gates.

All exact builds passed course-specific structural markers, scenario identity, changed-case evidence, invariant checks, source parsing, and native compiler gates. The Go gate compiles every exact build with Go 1.26.5; the TypeScript gate strict-checks the full source set with the current compiler surfaces. Both full first activities passed prediction, theory, keyboard ordering, exact code grading, isolated language execution or compiler diagnostics, inspection, debugging, and causal reflection. Course and studio routes passed H1/H2 order, 44-pixel visible targets, reduced motion, horizontal overflow, framework overlay, console, and page-error checks at 1440x900 desktop, 768x1024 tablet, and 390x844 mobile.

Browser reload testing also exposed and repaired a platform resume gap. SQLite already persisted completion, XP, code files, choices, ordering, and written answers, but the studio rehydrated only code files. The server now restores every recoverable per-step choice, sequence, and reflection draft as well as cumulative code. Corrupt or incorrectly typed fields are ignored without discarding other valid drafts. Regression tests cover full restoration and partial recovery, and live reload checks proved 7/7 completion, exact selected choices, exact keyboard order, and written reflection recovery.

## Safe evaluated Python AI-agent release evidence

Build and Ship a Safe Evaluated AI Agent with Python 3.14 and Gemini 3.5 has 30 prerequisite-ordered modules, 150 competencies, 306 activities, 2,494 learner steps, 2,740 checks, 246 exact runnable Python evidence builds, and five cumulative stakeholder projects. It starts after Python Basics, Python Functional Programming, HTTP Clients in Python, Prompt Engineering with Claude and Codex, Agent Loops and Goals, and Git Foundations. It then progresses through product and risk contracts, reproducible runtime evidence, model and API capability selection, deterministic provider ports, interaction state, strict structured output, tool schemas and dispatch, least authority, complete tool turns, bounded loops, structured concurrency, resumable human approval, checkpoints and replay, context and memory policy, retries and idempotency, accessible streaming, injection and exfiltration defense, privacy and provider terms, representative evaluations, trajectory evidence, calibrated rubrics, privacy-safe observability, capacity and cost, ADK conformance, contained delegation, sandbox boundaries, packaging, deployment, canary, kill switch, rollback, recovery, and residual-risk defense.

Research was reviewed 2026-07-14 and 2026-07-15 against Python 3.14.6; google-genai 2.11.0; the generally available Gemini Interactions API and current Gemini 3.5 model, function-calling, tools, structured-output, safety, data, terms, rate-limit, and caching guidance; google-adk 2.4.0 and its evaluation documentation; Pydantic 2.13.4; JSON Schema Draft 2020-12; pytest 9.1.1; Vertex AI Agent Engine; OWASP AI Agent Security and the Top 10 for Agentic Applications 2026; NIST AI 600-1; WCAG 2.2; current Python packaging guidance; Git 2.55; ACM/IEEE/AAAI CS2023; and the ACM Code of Ethics. All learner-facing material, cases, code, assessments, and projects are original. The course applies earlier prompt and loop courses instead of duplicating them, and makes model output, tool arguments, retrieved content, memory, and generated actions untrusted until independently validated and authorized.

The initial proposed runtime set exposed real upgrade fallout: google-adk 2.4.0 requires OpenTelemetry API and SDK at or below 1.42.1, so OpenTelemetry 1.43.0 could not resolve. The course and source register now use OpenTelemetry Python SDK 1.42.1 as the newest mutually compatible ADK runtime, with the incompatibility recorded rather than hidden. A disposable local-only Python 3.14.6 environment successfully installed and imported google-genai 2.11.0, google-adk 2.4.0, Pydantic 2.13.4, pytest 9.1.1, jsonschema 4.26.0, and OpenTelemetry SDK 1.42.1. Native smokes exercised strict Pydantic rejection, Draft 2020-12 schema validation, a typed Gen AI function declaration, a no-request Interactions client lifecycle, an ADK Agent and confirmation-capable FunctionTool construction, in-memory OpenTelemetry spans, and Python 3.14 TaskGroup structured concurrency without contacting a provider or sending the fake key.

All 246 exact builds are unique below the 0.90 near-duplication ceiling. The content gate maps all 30 modules to distinct evidence profiles, parses 702 Python starter and lesson sources, default-runs 396 lesson evidence functions with changed-case and invariant assertions, rejects inherited HTTP, RAG, crawler, static-site, game, and other course-cycle language, and keeps browser work deterministic and free of provider calls, native SDK claims, sockets, files, credentials, generated-code execution, browsers, databases, telemetry backends, and production effects. The first activity passed all seven prediction, theory, keyboard ordering, exact Python build, isolated Python execution, inspection, debugging, and causal-reflection layers. Course and activity routes passed meaningful-content, landmark, heading, horizontal-overflow, framework-overlay, page-error, and console checks at 1440x900 desktop, 768x1024 tablet, and 390x844 mobile.

## Authorized Python crawler and accessible site-auditor release evidence

Build and Ship an Authorized Web Crawler and Accessible Site Auditor with Python 3.14 has 30 prerequisite-ordered modules, 150 competencies, 306 activities, 2,494 learner steps, 2,740 checks, 246 exact runnable Python evidence builds, and five cumulative stakeholder projects. It starts after Python Basics, Python Data Structures and Algorithms, HTTP Clients in Python, Responsive Web Design, and Git Foundations, then progresses through authorization and outcomes, repository and runtime evidence, crawl scope, URL identity, robots and sitemaps, HTTP fetching, bytes and encoding, HTML parsing and selectors, typed records and provenance, link discovery and crawl graphs, trap control, politeness, structured concurrency, session boundaries, checkpoints and caches, deduplication and change, indexing metadata, structured data, accessibility, performance, dynamic rendering, block and anti-bot boundaries, untrusted-content security, accessible reporting, deterministic tests and faults, Scrapy scaling, packaging and operations, recovery, release, and production defense.

Research was reviewed 2026-07-14 against Python 3.14.6; Beautiful Soup 4.15.0; lxml 6.1.1; HTTPX 0.28.1; Scrapy 2.17.0; Playwright 1.61.0; RFC 9309 and RFC 9110/9111; the HTML and URL Living Standards; Selectors Level 4; the Sitemaps protocol; current Google Search, canonicalization, hreflang, and structured-data guidance; WCAG 2.2 and WAI image guidance; OWASP SSRF guidance; NIST privacy guidance; Git 2.55; and ACM/IEEE/AAAI CS2023. All learner-facing explanations, cases, and projects are original. The curriculum requires explicit authority, purpose limitation, provenance, rate limits, refusal and escalation evidence, and prohibits CAPTCHA or block evasion, proxy or identity rotation, credential theft, destructive methods, and personal-data hoarding. It does not claim that automation alone proves accessibility conformance.

All 246 exact builds are unique below the 0.90 near-duplication ceiling. The content gate maps all 30 modules to distinct evidence profiles, parses 702 Python starter and lesson sources, default-runs 396 lesson evidence functions with changed-case and invariant assertions, rejects inherited generic HTTP evidence-cycle language, and keeps browser models deterministic and free of sockets, DNS, native libraries, files, browsers, host schedulers, and production claims.

Current native compatibility was proven in disposable local-only environments, not inferred. Official Python 3.14.6 source was checksum-verified and used with HTTPX 0.28.1, Beautiful Soup 4.15.0, lxml 6.1.1, Scrapy 2.17.0, and Playwright 1.61.0. Native smokes exercised HTTPX mock transport and a loopback-only HTTP server, malformed HTML under both parser paths, robots and sitemap policy, Scrapy response and spider behavior, structured async concurrency, Chromium JavaScript rendering, browser-context isolation, and clean shutdown without contacting a public target. Playwright required its Ubuntu 24.04 fallback Chromium package on this host because the host OS is not officially supported; that browser smoke passed, but other target operating systems remain transfer gates.

The first learner activity passed all seven prediction, theory, inspection, keyboard ordering, exact Python build, isolated Python execution, debugging, and causal-reflection layers. Its live Pyodide evidence returned `(True, 8, 'authorized-crawl-charter')`; completion reported 7/7 layers and queued retrieval reviews for 1, 7, 21, and 60 days. Course and activity routes passed meaningful-content, landmarks, heading order, 44-pixel target, visible 3-pixel keyboard-focus, reduced-motion, horizontal-overflow, framework-overlay, page-error, and console checks at 1440x1000 desktop, 768x1024 tablet, and 390x844 mobile.

## Accessible Python and Tkinter maze solver release evidence

Build and Ship an Accessible Maze Solver with Python 3.14 and Tkinter has 24 prerequisite-ordered modules, 120 competencies, 246 activities, 2,006 learner steps, 2,204 checks, 198 exact runnable Python evidence builds, and five cumulative stakeholder projects. It starts after Python Basics and Python Data Structures and Algorithms, then progresses through player outcomes and repository evidence, Python and Tcl/Tk runtime boundaries, grid and wall invariants, Canvas rendering, responsive layout, keyboard input and focus, maze graphs, seeded generation and bias, DFS, BFS, Dijkstra, A-star, comparative evidence, cancellable `after` animation, state machines, pure-core architecture, accessible multichannel controls, deterministic replay and save data, property and native tests, profiling, packaging, local-file security, recovery, release, and production defense.

Research was reviewed 2026-07-14 against Python 3.14.6 and current `tkinter`, `collections.deque`, `heapq`, `random`, JSON, testing, and profiling documentation; Tcl/Tk 8.6.18 Canvas, binding, timer, and layout manuals; the original A-star and Wilson uniform-spanning-tree papers; WCAG 2.2; Xbox Accessibility Guidelines 3.2; PyInstaller 6.21.0; the Python Packaging User Guide; Git 2.55; and ACM/IEEE/AAAI CS2023. All learner-facing explanations, cases, and projects are original. All 198 exact builds are unique below the 0.90 near-duplication ceiling. The content gate parses 564 Python starter and lesson sources and default-runs 318 lesson evidence functions with changed-case and invariant assertions.

Current native compatibility was proven in disposable environments, not inferred. Official Python 3.14.6 source was checksum-verified, built against system Tcl/Tk 8.6.17, and passed a native Canvas, ttk Button, timer callback, and clean-shutdown smoke under Xvfb. PyInstaller 6.21.0 then built that same Python 3.14.6 and Tcl/Tk 8.6 application and the packaged artifact launched successfully under Xvfb. A separate Python 3.14.5 and Tcl/Tk 9.0 probe failed after PyInstaller did not collect `libtcl9.0.so` and `libtcl9tk9.0.so`; therefore Tk 9 packaging remains an explicit unverified boundary rather than a compatibility claim.

The first learner activity passed all seven prediction, theory, inspection, keyboard ordering, exact Python build, isolated Python execution, debugging, and causal-reflection layers. The runnable evidence returned `(True, 8, 'bounded-maze-charter')`, and completion reported 7/7 layers with a polite announced status. Live inspection found inherited HTTP request/response wording in the shared ordering generator; the Maze-specific generator path was repaired, all 226 affected generated activity files were regenerated, the stale phrase scan returned zero, and regression tests now reject HTTP evidence-cycle language in this course. Course and activity routes passed meaningful-content, landmarks, heading, 44-pixel target, visible 3-pixel keyboard-focus, reduced-motion, horizontal-overflow, framework-overlay, page-error, and console checks at 1440x1000 desktop, 768x1024 tablet, and 390x844 mobile.

## Production static-site generator with Python release evidence

Build and Ship a Production Static Site Generator with Python 3.14 has 24 prerequisite-ordered modules, 120 competencies, 246 activities, 2,006 learner steps, 2,204 checks, 198 exact runnable Python evidence builds, and five cumulative stakeholder projects. It starts after Python Basics, Python OOP, Responsive Web Design, and Git Foundations, then progresses through reader outcomes and repository evidence, static HTTP and URL models, an import-safe atomic pipeline, bounded content discovery, Unicode and typed metadata, source-mapped trees, inline and block parsing, nested-container recovery, semantic context-safe HTML, strict Jinja templates, stable routes and redirects, navigation and derived publications, fingerprinted assets, clean-equivalent incremental builds, accessibility, responsive performance, security, layered testing, diagnostics, packaging, reproducibility, GitHub Pages deployment, migration, rollback, recovery, and production defense.

Research was reviewed 2026-07-14 against Python 3.14.6 and current `pathlib`, `html`, `urllib.parse`, and `argparse` documentation; CommonMark 0.31.2; markdown-it-py 4.2.0 and its security guidance; Jinja 3.1.6 and MarkupSafe; the HTML and URL Living Standards; RFC 9110; WCAG 2.2 and WAI page-structure guidance; the CSS 2025 snapshot; OWASP XSS and file guidance; CSP Level 3; pytest 9.1.1; the current Python Packaging User Guide; reproducible-build guidance; Git 2.55; current GitHub Pages publication guidance; and ACM/IEEE/AAAI CS2023. The educational parser is explicitly a bounded original subset and never claims full CommonMark conformance. All 198 exact builds are unique below the 0.90 near-duplication ceiling. The content gate parses 564 Python starter and lesson sources and default-runs 318 lesson evidence functions with changed-case and invariant assertions.

The native compatibility smoke used a disposable environment with Python 3.14.5, markdown-it-py 4.2.0, Jinja 3.1.6, MarkupSafe 3.0.3, and pytest 9.1.1 because that was the available uv standalone Python 3.14 build. A safe markdown-it preset rejected raw HTML, Jinja strict autoescape encoded untrusted title and body data, and portable POSIX route composition passed. The curriculum targets current Python 3.14.6 documentation and reserves filesystems, full CommonMark, templates, validators, browsers, GitHub Actions, Pages, DNS, TLS, CDNs, caches, and production behavior for controlled transfer gates.

The first learner activity passed prediction, theory, inspection, exact code grading, debugging, keyboard ordering, reflection, and live isolated Python execution. Its changed case returned `(True, 8, 'bounded-site-charter')` and `(False, 8, 'accessibility-gap')`. Course and activity routes passed 24-module rendering, landmarks, heading order, visible 3-pixel keyboard focus, touch-target, horizontal-overflow, page-error, and console checks at 1440x1000 desktop, 768x1024 tablet, and 390x844 mobile.

## Accessible Asteroids with Python and pygame-ce release evidence

Build and Ship Accessible Asteroids with Python 3.14 and pygame-ce 2.5.7 has 24 prerequisite-ordered modules, 120 competencies, 246 activities, 2,006 learner steps, 2,204 checks, 198 exact runnable Python evidence builds, and five cumulative stakeholder projects. It starts after Python Basics and Python OOP, then progresses through game outcomes and repository evidence, SDL and the main loop, coordinate and vector systems, fixed-step simulation, events and remappable input, scenes and state, ship physics, wrapping and resize, rendering and assets, projectiles and pools, procedural asteroids and difficulty, collision detection and response, scoring and spawning, sprite ownership, audio cues, accessible HUD and menus, deterministic replay and save data, pure-core architecture, headless testing, performance, packaging, security, recovery, release, and production defense.

Research was reviewed 2026-07-14 against Python 3.14.6; pygame-ce 2.5.7 and its event, timing, vector, display, surface, drawing, image, transform, rectangle, sprite, mask, mixer, joystick, and controller APIs; SDL 2 behavior exposed by pygame-ce; PyInstaller 6.21.0; Git 2.55; Microsoft Xbox Accessibility Guidelines for input, multichannel cues, audio, difficulty, focus, motion, and photosensitivity; WCAG 2.2; the Python Packaging User Guide; and ACM/IEEE/AAAI CS2023. Classic pygame 2.6.1 does not publish Python 3.14 wheels, so the course uses pygame-ce. All learner-facing material and projects are original. All 198 exact builds are unique below the 0.90 near-duplication ceiling. The content gate parses 564 Python starter and lesson sources and default-runs 318 lesson evidence functions with changed-case and invariant assertions.

The native compatibility smoke used a disposable environment with Python 3.14.5, pygame-ce 2.5.7, and SDL 2.32.10 because that was the available uv standalone Python 3.14 build. Dummy video and audio drivers successfully exercised display presentation, the mixer, `Vector2`, masks, custom events, and sprite-group removal. The course still targets the current Python 3.14.6 documentation and records packaging as a platform-native build boundary rather than claiming cross-compilation.

The first learner activity passed prediction, theory, inspection, keyboard ordering, exact code grading, and live isolated Python execution through the first five pedagogical layers. Its changed case returned `(True, 8, 'bounded-game-charter')` and `(False, 8, 'accessibility-gap')`. Course and activity routes passed 24-module rendering, landmarks, heading order, visible keyboard focus, touch-target, horizontal-overflow, page-error, and console checks at 1440x1000 desktop, 768x1024 tablet, and 390x844 mobile.

## Unicode-safe Bookbot with Python release evidence

Build and Ship a Unicode-Safe Bookbot with Python 3.14 has 18 prerequisite-ordered modules, 90 competencies, 186 activities, 1,519 learner steps, 1,669 checks, 150 exact runnable Python evidence builds, and five cumulative stakeholder projects. It starts after Python Basics and Git Foundations, then progresses through outcome and repository evidence, CLI lifecycle contracts, path and file admission, text I/O and encoding, Unicode normalization, tokenization policy, frequency counting, deterministic ranking, character and grapheme metrics, bounded-memory streaming, domain-model pipelines, accessible reports, errors and exit status, `argparse`, fixtures and tests, profiling, packaging, release, security, recovery, and production defense.

Research was reviewed 2026-07-14 against Python 3.14.6 and its `pathlib`, `io`, `argparse`, `collections`, sorting, `unittest.mock`, profiling, `tracemalloc`, and `hashlib` documentation; Unicode 17 and UAX #15 and #29 revision 47; current Python Packaging User Guide, PEP 621, and entry-point specifications; Git 2.55; WCAG 2.2; and ACM/IEEE/AAAI CS2023. All learner-facing material, fixtures, explanations, and projects are original. All 150 exact builds are unique below the 0.90 near-duplication ceiling. The content gate parses 426 Python starter and lesson sources and default-runs 240 lesson evidence functions using deterministic fixtures and changed-case assertions.

The first learner activity passed prediction, theory, keyboard ordering, inspection, debugging, exact code grading, and live isolated Python execution. Its changed case returned `(True, 6, 'bounded-charter')` and `(False, 6, 'corpus-authority')`. Course and activity routes passed 18-module rendering, landmarks, heading order, 44-pixel visible controls, visible 3-pixel keyboard focus, horizontal-overflow, page-error, and console checks at 1440x1000 desktop, 768x1024 tablet, and 390x844 mobile.

## Retrieval-augmented generation engineering release evidence

Retrieval-Augmented Generation Engineering with Python 3.14 has 30 prerequisite-ordered modules, 150 competencies, 306 activities, 2,494 learner steps, 2,740 checks, 246 exact runnable Python evidence builds, and five cumulative stakeholder projects. It starts after Python Basics and Python OOP, then progresses through task and evidence framing, information-retrieval foundations, provenance-preserving ingestion, Unicode normalization and tokenization, structure-aware chunking, TF-IDF and BM25, ranked retrieval metrics, embeddings, dense retrieval, exact and approximate indexes, vector-store lifecycle, hybrid fusion, query understanding, reranking, context selection, grounded generation and abstention, accessible citations, pipeline architecture, component and end-to-end evaluation, evaluation datasets, security, privacy, observability, performance, continuous ingestion and recovery, structured and GraphRAG, agentic and adaptive RAG, multimodal RAG, architecture evolution, testing, release, rollback, and production defense.

Research was reviewed 2026-07-14 against Python 3.14.6, NumPy 2.5.1, scikit-learn 1.9.0, sentence-transformers 5.6.0, FAISS 1.14.3, pgvector 0.8.2, current OpenAI retrieval and Microsoft GraphRAG documentation, NIST RAG and AI risk-management work, OWASP RAG and LLM security guidance, OpenTelemetry semantic conventions 1.43, Unicode 17 normalization and segmentation standards, WCAG 2.2, and primary RAG, DPR, HNSW, reciprocal-rank fusion, ColBERT, HyDE, lost-in-the-middle, BEIR, MTEB, RAG-evaluation, and multimodal retrieval papers. All 246 exact builds are unique below the 0.90 near-duplication ceiling. The content gate parses 702 Python starter and lesson sources, default-runs 396 lesson evidence functions, and requires changed-query, changed-corpus, rejection or abstention, provenance, citation, evaluation, failure, recovery, and production-transfer evidence rather than accepting plausible prose.

Browser RAG evidence uses deterministic pure-Python functions and fixed fixtures; native ML packages, real models, vector databases, providers, files, OCR, load, faults, recovery systems, credentials, and production behavior remain explicit controlled transfer gates. The Python learning worker now rejects empty, oversized, and null-byte source, caps source at 32 KiB and output at 64 KiB, gives each run fresh globals, and terminates stalled work after eight seconds. The first learner activity passed prediction, theory, inspection, keyboard ordering, exact code grading, and live Python execution through the first five pedagogical layers. Its changed case returned `(True, 3, 'bounded-outcome')` and `(False, 0, 'missing-evidence')`. Course and activity routes passed 30-module rendering, landmarks, heading order, 44-pixel visible controls, visible 3-pixel keyboard focus, horizontal-overflow, page-error, and console checks at 1440x1000 desktop, 768x1024 tablet, and 390x844 mobile.

## Applied cryptographic engineering in Go release evidence

Applied Cryptographic Engineering in Go 1.26 has 28 prerequisite-ordered modules, 140 competencies, 286 activities, 2,331 learner steps, 2,561 checks, 230 exact runnable Go evidence builds, and five cumulative projects. It starts after Go Basics and progresses through threat models and evidence, canonical bytes and primitive selection, randomness and secret lifecycle, classical failure analysis, AES and modes, AEAD with AES-GCM and ChaCha20-Poly1305, hashes, HMAC, HKDF, password hashing, modular arithmetic, RSA, ECDH, signatures, HPKE and envelope systems, key management, X.509, TLS 1.3, protocol state machines, encrypted streams, post-quantum migration, side-channel and oracle defense, vectors and fuzzing, cryptographic agility and FIPS boundaries, operations, recovery, release, and production defense.

Research was reviewed 2026-07-14 against Go 1.26.5 and its cryptographic packages, golang.org/x/crypto 0.54.0, current Go HPKE, ML-KEM, ML-DSA, and FIPS 140 documentation, NIST FIPS 197/203/204/205 and SP 800-38D/57, RFC 8439/8446/9106, OWASP cryptographic guidance, and ACM/IEEE/AAAI CS2023. Unsafe legacy algorithms appear only in bounded attack, rejection, compatibility, and migration work. All 230 exact builds are unique below the 0.90 near-duplication ceiling; 70 standalone cryptographic API programs cover 43 distinct programs and compile under Go 1.26.5. The Go source audit parses and type-checks 1,508 starter and lesson sources across all released Go tracks.

Browser cryptography remains deterministic and isolated. The Yaegi 0.16.1 allowlist gained only side-effect-free standard-library cryptographic and byte-encoding packages; it still forbids OS randomness, TLS and network access, files, processes, environment state, shell commands, credentials, and external effects. Source is capped at 32 KiB, output at 64 KiB, and stalled runs stop after 12 seconds. A live AES-GCM exercise opened valid ciphertext and rejected a changed authentication tag with `opened=true plaintext=private-evidence tamper_rejected=true`. Course and activity routes passed landmark, heading, 44-pixel target, visible 3-pixel keyboard-focus, horizontal-overflow, page-error, and console checks at 1440x1000 desktop, 768x1024 tablet, and 390x844 mobile. Regression tests prevent inherited HTTP, RabbitMQ, or storage wording from entering the cryptography learning cycle.

## Modern C23 memory-safety release evidence

Memory Management and Memory Safety in Modern C23 has 28 prerequisite-ordered modules, 140 competencies, 286 activities, 2,333 learner steps, 2,563 checks, 230 exact runnable C evidence builds, and five cumulative projects. It starts after Python Data Structures and Algorithms and builds the C foundations required for memory work before progressing through translation and diagnostics, integer and size arithmetic, objects and lifetime, representation and alignment, arrays and pointers, strings and encoding, structures and tagged variants, callbacks and interfaces, automatic storage, heap allocation, overflow-safe formulas, ownership and borrowing, cleanup, transactional `realloc`, temporal and spatial defects, initialization and undefined behavior, aliasing and provenance, arenas and pools, reference counting, tracing collection, external resources, concurrency and safe reclamation, ABI and FFI, WebAssembly linear memory, sanitizers, static analysis, fuzzing, profiling, hardening, release, rollback, and incident defense.

Research was reviewed 2026-07-14 against ISO/IEC 9899:2024 and current WG14 material; ISO/IEC TS 6010:2025 provenance guidance; GCC 16.1; Clang 22.1.0; official AddressSanitizer, UndefinedBehaviorSanitizer, MemorySanitizer, LeakSanitizer, ThreadSanitizer, static-analyzer, and libFuzzer documentation; SEI CERT C; POSIX.1-2024; MITRE CWE; Python 3.14.6 C API and ABI documentation; Rust FFI; WebAssembly Core 3.0; ACM/IEEE CS2023; Boehm GC; mimalloc; and jemalloc. All 370 learner-facing C lesson programs compile warning-clean with the installed GCC 15.2 C99 practice gate, all 230 exact builds are unique below the 0.90 near-duplication ceiling, and one representative program from every module executes with its printed invariant equal to one. C23 language and library support remains a named current-compiler transfer gate rather than a browser claim.

Instant C practice runs in a disposable browser Web Worker using PicoC 3.2.2, rebuilt from its pinned upstream archive with Emscripten 6.0.3. Source is capped at 32 KiB, output at 64 KiB, a stalled run is killed after eight seconds, and learner input cannot reach host files, processes, environment, shell, network, native compiler, or server execution. The UI labels the interpreter as a C89/C90 plus selected C99 practice subset and explicitly reserves C23, GCC, Clang, optimization, ABI, threads, sanitizers, analyzers, fuzzers, operating-system resources, FFI, load, and production claims for controlled transfer gates.

The first learner activity passed prediction, concept, keyboard ordering, inspection, debugging, exact C grading, and actual browser Worker execution through the code layer; output reported `browser=2`, `complete=12`, and `invariant=1`. Course and activity routes passed heading, landmark, 44-pixel target, visible keyboard-focus, reduced-motion, horizontal-overflow, page-error, and console checks at 1440×1000 desktop, 768×1024 tablet, and 390×844 mobile. The generator now preserves shared course requirements in both blueprints and version 2 course records, and the curriculum schema accepts the one-character `c` code-language identifier with regression coverage.

## Amazon S3 and CloudFront with TypeScript release evidence

Production File Delivery with Amazon S3 and CloudFront in TypeScript 7 has 28 prerequisite-ordered modules, 140 competencies, 286 activities, 2,332 learner steps, 2,562 checks, 230 exact compile-ready TypeScript evidence builds, and five cumulative projects. It progresses from stakeholder outcomes, typed architecture decisions, HTTP representation and Node stream boundaries, S3 objects, bucket types and S3 Express, modular AWS SDK v3 clients and commands, runtime validation, credentials, retries, bounded upload admission, integrity, `@aws-sdk/lib-storage`, multipart state, direct browser upload, downloads, Node gateways, and PostgreSQL reconciliation through object mutations, versioning, Object Lock, lifecycle, IAM, SSE-KMS, S3 and CloudFront signing, OAC, cache identity, accessible media and ranges, edge security, replication and disaster recovery, events, observability, capacity, cost, tests, faults, rollback, and production defense.

Research was reviewed 2026-07-14 against official Node.js 24.18.0 documentation; the TypeScript 7.0.2 native CLI; the `@typescript/typescript6` package 6.0.2, whose bundled compiler reports 6.0.3; AWS SDK for JavaScript v3.1087.0 and its current S3 client, lib-storage, request-presigner, and CloudFront signer packages; current official S3 and CloudFront documentation; RFC 9110; and ACM/IEEE-CS/AAAI CS2023. All exact builds compile under both installed compiler paths, remain unique below the 0.90 near-duplication ceiling, and distinguish deterministic browser decisions from controlled Node, AWS, network, stream, IAM, KMS, WAF, database, load, cost, restore, and production transfer gates. Cross-course gates reject repeated Go/TypeScript module titles and normalized competency duplication.

The first production activity passed all seven prediction, theory, keyboard arrangement, exact code, strict compiler, inspection, debugging, and causal-reflection layers. A 269-character reflection that omitted required `strict` and `node` causal terms was rejected before the repaired evidence passed. Course and studio routes passed title, 28-module, heading-hierarchy, 44-pixel control, overflow, console, request, focus, and reduced-motion checks at 1440×900 desktop, 768×1024 tablet, and 390×844 mobile. Activity pages now expose one screen-reader-visible H1 above the current-step H2. All observed draft, attempt, and TypeScript-runtime requests returned 200, and the browser reported no console errors.

## Amazon S3 and CloudFront with Go release evidence

Production File Delivery with Amazon S3 and CloudFront in Go 1.26 has 28 prerequisite-ordered modules, 140 competencies, 286 activities, 2,332 learner steps, 2,562 checks, 230 exact compile-ready Go evidence builds, and five cumulative projects. It progresses from stakeholder outcomes, storage architecture, HTTP representation and range semantics, S3 objects, bucket types, S3 Express directory buckets, AWS SDK for Go v2 contracts, credentials, retries, bounded upload admission, PutObject integrity, transfer-manager adoption, multipart state, direct browser uploads, downloads, and streaming Go gateways through database and object reconciliation, conditional mutations, versioning, Object Lock, lifecycle and archive restore, IAM and bucket security, SSE-KMS, signed access, OAC, CloudFront cache identity, accessible media and range delivery, edge security, replication and disaster recovery, events, observability, capacity, cost, tests, faults, rollback, and production defense.

Research was reviewed 2026-07-14 against official Go 1.26.5 documentation; aws-sdk-go-v2 1.42.1; S3 service module 1.105.0; the new pre-v1 `feature/s3/transfermanager` 0.3.2; deprecated `feature/s3/manager` 1.22.33 migration guidance; CloudFront sign 1.11.7 with explicit `HashSHA256`; current official S3 consistency, object, directory-bucket, integrity, multipart, presigning, CORS, versioning, Object Lock, lifecycle, storage-class, security, encryption, and replication documentation; current official CloudFront OAC, private-content, cache, range, invalidation, and security documentation; RFC 9110; and ACM/IEEE-CS/AAAI CS2023. Exact builds parse and compile together under Go 1.26.5, remain unique below the 0.90 near-duplication ceiling, and distinguish deterministic browser decisions from controlled AWS, network, IAM, KMS, WAF, race, leak, load, cost, restore, and production transfer gates.

Browser exercises remain deterministic pure Go: they do not import AWS SDK or network, TLS, host-process, or database packages; contact S3, CloudFront, KMS, IAM, WAF, networks, databases, or billing; read host state; execute host commands; or use credentials. The first production activity passed all seven prediction, theory, keyboard ordering, inspection, debugging, exact code, isolated Go 1.26.5 execution, and causal-reflection layers; the server correctly rejected a 218-character reflection missing the required `file` and `delivery` causal terms before accepting the repaired evidence. Course and studio routes passed 28-module rendering, overflow, control-size, console, request, keyboard-focus, and reduced-motion checks at 1440×900 desktop, 768×1024 tablet, and 390×844 mobile. The full non-Lighthouse gate passed 57 test files and 360 tests, type-check, normal and strict lint across 243 files, and the Next.js 16.2.10 production build.

## RabbitMQ with Go release evidence

RabbitMQ 4.3 Reliable Messaging with Go 1.26 has 27 prerequisite-ordered modules, 135 competencies, 276 activities, 2,249 learner steps, 2,471 checks, 222 exact compile-ready Go evidence builds, and five cumulative messaging projects. It progresses from honest delivery claims, architecture selection, the AMQP model, amqp091-go contracts, connections, channels, recovery, topology, work queues, fanout, topic and headers routing, envelopes, backpressure, returns, confirms, consumer goroutines, acknowledgements, prefetch, idempotency, typed retry policy, dead lettering, and quorum queues through RabbitMQ 4.3 delayed retries, priorities and consumer timeouts, ordering, streams and super streams, outbox and sagas, security, observability, capacity, race and chaos testing, upgrades, recovery, and production defense.

Research was reviewed 2026-07-14 against official RabbitMQ 4.3.2 architecture, reliability, queue, exchange, confirm, consumer, quorum, dead-lettering, flow-control, security, stream, release, and upgrade documentation; official rabbitmq/amqp091-go 1.12.0 API and design documentation; Go 1.26.5 language, memory-model, race, testing, fuzzing, pprof, and trace documentation; the RabbitMQ-maintained Stream Go client; AsyncAPI 3.1.0; CloudEvents 1.0.2; and OpenTelemetry semantic conventions 1.43.0. Exact builds parse and compile together under Go 1.26.5, produce changed-case messaging evidence, remain unique below the 0.90 near-duplication ceiling, and distinguish browser models from authorized amqp091-go, live-broker, TLS, partition, restart, Docker, database, race, leak, load, upgrade, and production transfer gates.

Browser exercises remain deterministic pure Go: they do not import amqp091-go or network, TLS, host-process, or database packages; open sockets; contact RabbitMQ, Docker, networks, or databases; read host state; execute host commands; or use credentials. The first production activity passed all seven prediction, theory, inspection, keyboard ordering, exact Go build, isolated Go 1.26.5 execution, debugging, and causal-reflection layers. The generator now removes stale module and activity JSON after renamed competencies, and aggregate tests require exact filesystem-to-graph parity. Course and studio routes passed 27-module rendering, 44-pixel targets, overflow, overlay, console, request, keyboard-focus, and reduced-motion checks at 1440×900 desktop, 768×1024 tablet, and 390×844 mobile.

## RabbitMQ with TypeScript release evidence

RabbitMQ 4.3 Pub/Sub and Reliable Messaging with TypeScript 7 has 27 prerequisite-ordered modules, 135 competencies, 276 activities, 2,257 learner steps, 2,479 checks, 222 exact TypeScript evidence builds, and five cumulative messaging projects. It progresses from delivery claims, architecture selection, the AMQP model, amqplib contracts, connections, channels, topology, work queues, fanout, topic, headers, envelopes, backpressure, mandatory returns, confirms, consumers, acknowledgements, prefetch, idempotency, retries, dead lettering, and quorum queues through RabbitMQ 4.3 delayed retries, strict priorities and consumer timeouts, ordering, streams and super streams, transactional outbox and sagas, security, observability, capacity, chaos, upgrades, recovery, and production defense.

Research was reviewed 2026-07-14 against current official RabbitMQ 4.3.2 architecture, reliability, queue, exchange, confirm, consumer, quorum, dead-lettering, flow-control, security, stream, release, and upgrade documentation; amqplib 2.0.1; Node.js 24.18.0; TypeScript 7.0.2 and the TypeScript 6 compatibility compiler; AsyncAPI 3.1.0; CloudEvents 1.0.2; and OpenTelemetry semantic conventions 1.43.0. Exact builds compile with both TypeScript compiler paths, return changed-case messaging evidence, stay below the 0.90 near-duplication ceiling, and name authorized amqplib, broker, TLS, partition, restart, Docker, database, load, upgrade, or production transfer gates.

Browser exercises remain deterministic pure TypeScript: they do not import amqplib or Node built-ins, open sockets, contact RabbitMQ, Docker, networks or databases, read host state, execute host commands, or use credentials. The first production activity passed all seven prediction, theory, inspection, exact code, compiler, debugging, ordering, and causal-reflection layers. The generator regression now prevents TypeScript messaging explanations from leaking internal `rabbit-ts` labels or claiming Go execution evidence. Fresh production routes passed content, touch-target, overflow, overlay, console, page-error, keyboard-focus, and reduced-motion checks at 1440×900 desktop, 768×1024 tablet, and 390×844 mobile.

## HTTP Protocol in Go release evidence

HTTP Protocol Engineering in Go 1.26 has 24 prerequisite-ordered modules, 120 competencies, 246 activities, 2,006 learner steps, 2,204 checks, 198 exact Go evidence builds, and five cumulative protocol projects. It progresses from octets, partial I/O, TCP, semantics, ABNF, request targets, fields, framing, content, response serialization, persistence, expectations, intermediaries, authority, TLS 1.3, ALPN, Upgrade, and CONNECT through smuggling defense, slow-client limits, explicit state machines, differential and fuzz testing, HTTP/2 with HPACK, and HTTP/3 with QUIC and QPACK. The protocol-transition module incorporates the March 2026 RFC 9931 requirement not to send unsafe optimistic new-protocol bytes before an Upgrade or CONNECT transition is accepted.

Research was reviewed 2026-07-14 against current IETF HTTP, TCP, TLS, ALPN, HPACK, QUIC, QPACK, Structured Fields, and protocol-transition RFCs; Go 1.26.5 `net`, `bufio`, `io`, `crypto/tls`, `net/textproto`, testing, fuzzing, and security documentation; and ACM/IEEE/AAAI CS2023. Exact Go builds compile, produce changed-case evidence, preserve resource and state invariants, remain unique below the near-duplication ceiling, and name their authorized net.Pipe, loopback, TLS, packet-capture, proxy, fuzz, race, load, HTTP/2, HTTP/3, or production transfer gate. Browser Go remains a bounded deterministic model and cannot open sockets, negotiate TLS, contact networks or proxies, capture packets, read host state, execute host commands, or use credentials.

The generator now strips the internal `httpproto-` namespace from learner-facing titles, summaries, artifacts, and explanatory text while retaining exact lowercase markers only inside code-evidence instructions. Regression tests enforce that boundary. The full first activity passed all seven interactions and isolated Go execution before the label-only repair; the rebuilt production route then passed label, content, focus, reduced-motion, touch-target, overflow, overlay, console, and page-error checks at 1440×900 desktop, 768×1024 tablet, and 390×844 mobile.

## HTTP Servers in TypeScript release evidence

HTTP Servers in TypeScript: Node 24 and Express 5 Production APIs has 24 prerequisite-ordered modules, 120 competencies, 246 activities, 2,006 learner steps, 2,204 checks, 198 exact TypeScript evidence builds, and five cumulative production-service projects. It starts after both HTTP Clients in TypeScript and SQL, then moves through outcomes and the compiler/runtime contract, Node lifecycle, HTTP semantics, core Node HTTP, Express 5 routing and middleware, Zod 4 validation, authority and proxy trust, caching and representations, bounded bodies and uploads, problem responses and streaming, async context and cancellation, concurrency and overload, PostgreSQL pools and transactions, consistent resource APIs, authentication and JOSE, authorization and tenancy, browser trust, webhooks and SSRF, proxy/framing defense, testing and faults, observability, capacity, OpenAPI 3.2, release, recovery, and production defense.

Research was reviewed 2026-07-14 against official Node.js 24.18 documentation; TypeScript 7.0.2 native compiler and TypeScript 6.0.2 compatibility API documentation; Express 5.2.1, Zod 4.4.3, node-postgres 8.22.0, jose 6.2.3, and OpenTelemetry 1.43 documentation; OpenAPI 3.2.0; PostgreSQL 18.4; current HTTP RFCs; and current OWASP API and server guidance. All 198 exact builds compile under both TypeScript toolchains, use unique exact evidence contracts, stay below 0.90 near-duplication, return changed-case behavior, and name authorized Node, Express, listener, TLS, PostgreSQL, proxy, worker, load, or production transfer gates.

Browser exercises remain deterministic pure TypeScript: they do not import Node or Express, open listeners, contact networks or databases, spawn workers, read host state, or use credentials. The production first activity passed prediction, theory, keyboard ordering, exact code grading, the isolated TypeScript check, inspection, debugging, and causal reflection. Course and studio checks passed at 1440x900 desktop, 768x1024 tablet, and 390x844 mobile with zero page overflow, no undersized visible controls, no console or page errors, visible keyboard focus, and reduced-motion behavior. The shared course journey now enforces 44-pixel brand and module-action targets with regression tests.

## Node 24 and npm 12 toolchain evidence

The application, clean install, lockfile, and container now use Node.js 24.18.0 LTS and npm 12.0.1. Exact lifecycle approvals are limited to agent-browser 0.31.2, better-sqlite3 12.11.1, esbuild 0.28.1, and the nested Next.js sharp 0.34.5 package. The npm 12 strict policy permits HTTPS registry tarballs and root workspace/file dependencies while rejecting Git dependencies; tests also require every remote lockfile URL to remain under `https://registry.npmjs.org/`. This is required because npm 12.0.1 currently classifies Tailwind's bundled registry-hosted WASM tarball as a remote dependency during package-lock-only tree expansion.

A fresh `npm ci` completed with zero reported vulnerabilities and no unapproved pending lifecycle scripts. Native smoke checks passed for better-sqlite3, sharp, esbuild, and agent-browser. The production Docker image built with Node 24.18.0/npm 12.0.1 and served a successful `/api/health` response.

## HTTP Servers in Go release evidence

HTTP Servers in Go 1.26 has 21 prerequisite-ordered modules, 105 competencies, 216 activities, 1,762 learner steps, 1,936 checks, 174 exact Go evidence builds, and five cumulative production-service projects. It starts after both HTTP Clients in Go and SQL, then moves through lifecycle and service outcomes, HTTP resource semantics, ServeMux routing, handler and middleware architecture, request authority and proxy trust, representations and caching, bounded bodies, problem responses and streaming, context and cleanup, HTTP/1.1 and HTTP/2 server configuration, concurrency and overload, PostgreSQL repositories and transactions, resource consistency, authentication, authorization and tenancy, browser trust, webhooks and SSRF, reverse proxies and request-smuggling defense, tests and fuzzing, observability and capacity, OpenAPI 3.2 compatibility, deployment, recovery, and production defense. HTTP/3 is explicitly outside the standard `net/http` server boundary.

Research was reviewed 2026-07-14 against Go 1.26.5 `net/http`, `httptest`, `httputil`, and `database/sql`; RFC 9110, RFC 9111, RFC 9112, RFC 9457, RFC 9700, and RFC 7519; OWASP API Security Top 10 2023 and REST Security guidance; PostgreSQL 18.4; OpenTelemetry HTTP semantic conventions 1.43.0; OpenAPI 3.2.0; and current Go security practice. Every build uses one of 21 module-specific compile-ready pure-Go decision contracts with a competency marker, scenario identity, changed case, observable return, and explicit listener, TLS, database, proxy, race, load, or production transfer limit. All 174 exact builds are unique, exact regex contracts pass, near-duplication stays below 0.90, all 279 learner-facing Go lesson blocks parse, and all exact build functions compile together under Go 1.26.5.

The browser course never imports `net/http`, `net`, `database/sql`, `os`, or host execution. It models bounded request, handler, dependency, response, and cleanup decisions in the isolated Go WebAssembly worker. The production learner flow passed prediction, theory, inspection, exact code grading, Go execution, debugging, keyboard-operable ordering, and causal reflection. Catalog and studio checks passed at 1440x900 desktop, 768x1024 tablet, and 390x844 mobile without overflow, undersized visible controls, console errors, or framework overlays; keyboard focus used a visible 3-pixel outline, and reduced motion disabled active animation and normal-duration transitions.

## Advanced Git release evidence

Advanced Git 2.55 has 20 prerequisite-ordered modules, 100 competencies, 206 activities, 1,678 learner steps, 1,844 checks, 166 repository evidence builds, and five cumulative stakeholder projects. It deliberately starts after Git Foundations and moves through advanced refs and revision algebra, object storage and integrity, reflog recovery, layered undo, merge topology, conflict stages and rerere, rebase and replay, backports, stash and patch transfer, bisect, worktrees, refspecs and leases, maintainer workflows, signatures and releases, submodules, partial clone and sparse checkout, attributes and filters, repository trust, maintenance and bundles, SHA-256 transition, Git 3.0 readiness, and disaster defense.

Research was reviewed 2026-07-14 against current official Git 2.55.0 documentation, including revision, repository layout, fsck, reflog, reset/restore/revert, merge, rerere, rebase, cherry-pick, replay, range-diff, bisect, worktree, fetch/push, maintainer workflows, signatures, submodules, partial clone, sparse checkout, attributes, configuration, hooks, credentials, maintenance, commit-graph, bundle, hash-function transition, and planned breaking-change references. All 166 build increments require exact before and after refs, a falsifiable hypothesis, a bounded Git command, expected and observed state, changed-case evidence, tree and behavior proof, a recovery anchor, and a real-repository transfer limit. Topic-specific misconception checks prevent null or generic distractors; exact and near-duplication gates keep evidence examples below 0.90 similarity.

The terminal runtime now models advanced repository state and transitions for revision queries, object integrity, reflog and reset, merge bases and conflict stages, rerere, rebase and range-diff, cherry and replay, stash, bisect, worktrees, explicit force-with-lease, tags and signatures, submodules, sparse checkout, attributes, configuration origins, maintenance, bundles, and SHA-256 format evidence. It remains deterministic and disposable: learner input never invokes host Git, files outside the scenario, credentials, hooks, repository executables, or network remotes. Live history editing, remote publication, historical-code execution, signing, hosting, and object-format migration remain named authorized transfer gates.

## CI/CD release evidence

Secure CI/CD with GitHub Actions, TypeScript, Docker, and Cloud Run has 20 prerequisite-ordered modules, 100 competencies, 206 activities, 1,676 learner steps, 1,842 checks, and five cumulative stakeholder projects. Coverage moves from delivery outcomes, execution state, workflow syntax, event trust, and runner isolation through reproducible Node 24 builds with the TypeScript 7.0.2 native CLI and TypeScript 6.0.2 API compatibility tooling, gates, tests, matrices, artifacts, caches, concurrency, permissions, action supply chain, reuse, Docker Buildx, attestations, OIDC, Cloud Run progressive delivery, trusted publishing, incidents, and governance.

Research was reviewed 2026-07-14 against current official GitHub Actions and action-release documentation; Node 24 LTS, TypeScript 7.0.2 native compiler and @typescript/typescript6 6.0.2 compatibility documentation, and npm 11; Docker Build GitHub Actions; SLSA 1.2; npm trusted publishing; Google authentication and Cloud Run deployment documentation; NIST SSDF 1.1; and ACM/IEEE/AAAI CS2023. All 166 build increments have module-specific workflow or release artifacts, changed environments, fault injection, retained proof, deterministic verification, rejected failures, repairs, and explicit authorized disposable-environment transfer limits. Exact and near-duplication gates keep same-kind activities and evidence examples below 0.90 similarity.

The CI/CD workspace is intentionally a deterministic text analyzer. It never calls GitHub APIs, starts runners, executes learner commands, accesses Docker or registries, exchanges OIDC tokens, or contacts cloud services. Its accessible contract report checks workflow and revision identity, event trust, permissions, runner and toolchain, gates and tests, state handoff, bounded execution, supply chain, image release, cloud identity, progressive recovery, verification, failure, repair, and transfer evidence. Live delivery behavior remains a named transfer gate in an authorized disposable repository, runner, registry, and cloud project.

## Kubernetes release evidence

Modern Kubernetes 1.36 has 21 prerequisite-ordered modules, 105 competencies, 216 activities, 1,757 learner steps, 1,931 checks, and five cumulative stakeholder projects. Coverage moves from architecture, reconciliation, API objects, kubectl context safety, metadata, Pods, configuration, health, and controllers through Services, Gateway API, NetworkPolicy, storage, scheduling, availability, identity, admission, observability, packaging, extensions, upgrades, and disaster recovery.

Research was reviewed 2026-07-14 against Kubernetes 1.36.2, minikube 1.38.1, Gateway API 1.5.1, Helm 4.2.2, current official Kubernetes API, workload, networking, storage, scheduling, security, observability, packaging, extension, upgrade, and recovery documentation, plus ACM/IEEE/AAAI CS2023. All 174 build increments have module-specific object or operational artifacts, changed environments, fault injection, retained proof, deterministic verification, rejected failures, repairs, and explicit disposable-cluster transfer limits. Exact and near-duplication gates keep same-kind activities and evidence examples below 0.90 similarity.

The Kubernetes workspace is intentionally a deterministic text analyzer. It never invokes kubectl, reads kubeconfig, contacts a cluster API, or executes host commands. Its accessible contract report checks API object identity, desired versus observed state, immutable release, workload health, networking, storage, placement, identity and security, observability, verification, failure, repair, rollback, and transfer evidence. Live Kubernetes behavior remains a named transfer gate in an authorized disposable cluster.

## Docker release evidence

Modern Docker 29 has 18 prerequisite-ordered modules, 90 competencies, 186 activities, 1,514 learner steps, 1,664 checks, and five cumulative stakeholder projects. Coverage moves from architecture and environment evidence through lifecycle, images, Dockerfile, BuildKit, process behavior, storage, networking, Compose, security, secrets, diagnostics, testing, reproducibility, multi-platform builds, supply chain, and production operations.

Research was reviewed 2026-07-14 against Docker Engine 29.6.1, Compose 5.3.1, Buildx 0.35.0, BuildKit 0.31.1, Dockerfile frontend 1.25, current OCI specifications, official Docker security/storage/network/runtime/attestation documentation, and ACM/IEEE/AAAI CS2023. All 150 build increments have scoped module-specific artifacts, changed environments, fault injection, retained proof, deterministic verification, rejected failures, repairs, and explicit disposable-environment transfer limits. Exact and near-duplication gates keep same-kind activities and evidence examples below 0.90 similarity.

The Docker workspace is intentionally a deterministic text analyzer. It never connects to a Docker daemon, mounts a socket, or executes learner commands. Its accessible contract report checks artifact, immutable identity, build, lifecycle, state, least privilege, secrets, verification, failure, repair, and supply-chain evidence. Live Docker behavior remains a named transfer gate in an authorized disposable environment.

## HTTP client release evidence

Each HTTP client course has 18 modules, 90 competencies, 186 activities, 1,514 steps, 1,664 checks, and five project records. The three tracks share HTTP coverage while using language-specific authorities, executable evidence contracts, scenarios, runtime boundaries, projects, and transfer gates.

The HTTP release gates verify schema and prerequisite order; introduce/guided/faded/retrieve/assess/transfer coverage; distinct workshop, debugging, lab, review, quiz, project, and exam contexts; exact and near duplication; language-specific theory signatures; changed-case evidence; and strict source compilation. The near-duplication ceiling is below 0.93 similarity rather than exact-text-only review.

The Go audit parses and type-checks 852 starter and lesson sources across Go Basics and HTTP Clients in Go. The TypeScript audit strict-checks 426 HTTP sources; the Python audit parses 426 HTTP sources. The research register was reviewed 2026-07-14 against current HTTP semantics, caching, URI, cookies, and problem-details RFCs; current Go, TypeScript/Node, Python, and Fetch documentation; OWASP request-safety guidance; and OpenTelemetry conventions.

## Completed platform capabilities

- Curriculum blueprint schema checks IDs, prerequisite order, competency coverage, exact duplication, project references, and exactly one final-module exam.
- Learner studio supports varied prediction, inspection, arrangement, code, debugging, answer, calculation, and reflection steps.
- Hidden canonical grading stays server-side; public activity payloads strip answers.
- Python runs in a dedicated Pyodide worker with fresh globals per run, 32 KiB source and 64 KiB output limits, source validation, and an eight-second worker kill. Shell/network tasks use a safe simulator. Web output uses a restrictive iframe sandbox.
- SQL runs in a dedicated worker against a new in-memory database for every request. Learner SQL never reaches a host database or host command execution; execution has a three-second worker kill, bounded result rows, deterministic seed data, and semantic responsive result tables.
- Go runs in a dedicated browser worker against a locally bundled Go 1.26.5 WebAssembly host and Yaegi 0.16.1 interpreter. Each run receives a fresh interpreter, allows only a reviewed deterministic standard-library subset, cannot reach host files, processes, environment, or network, caps source/output, and kills stalled workers after 12 seconds.
- C runs in a dedicated disposable browser worker against locally bundled PicoC 3.2.2 WebAssembly rebuilt with Emscripten 6.0.3. It is explicitly a C89/C90 plus selected C99 instant-practice subset, caps source and output, kills stalled work after eight seconds, and cannot reach host files, processes, environment, shell, network, native compilers, or server execution.
- Attempt and draft schemas share one tested eleven-file contract, so HTML, CSS, JavaScript, TypeScript, Python, Go, C, SQL, shell, prompt, and config work survive grading and autosave without silent field stripping.
- Activity reloads restore cumulative code plus every recoverable per-step choice, keyboard order, and written response from SQLite. Invalid or corrupt draft fields are skipped independently so one bad field cannot erase other valid resume state.
- TypeScript uses the current native 7.0.2 CLI for repository type-checking while retaining the `@typescript/typescript6` package 6.0.2 for compiler-API consumers until TypeScript 7.1 provides the replacement API. That package's compiler reports version 6.0.3; course content and regression tests preserve the package-version versus compiler-version distinction.
- Linux, Git, and network commands use a stateful deterministic browser simulator that models disposable filesystem, foundational and advanced repository transitions, process, service, storage, and network evidence without executing learner input on the host.
- Prompt and config workspaces evaluate explicit contracts instead of accepting placeholder text.
- Docker config work receives a distinct safe contract review covering container delivery, failure, repair, and transfer evidence without host execution.
- Kubernetes config work receives a distinct safe contract review covering object state, controllers, networking, storage, security, failure, repair, rollback, and cluster-transfer evidence without host execution.
- CI/CD config work receives a distinct safe contract review covering workflow identity, trust, runners, gates, artifacts, supply chain, OIDC, deployment, recovery, and transfer evidence without external execution.
- Calculation checks support tolerance and required units.
- Browser smoke verification has passed on desktop, 390x844 mobile, and 768x1024 tablet for catalog, math, prompt, Linux terminal, foundational and Advanced Git, runnable SQL, cumulative Go, cryptography, C, RAG, Unicode-safe Bookbot, accessible Asteroids, production static-site generator, accessible Python and Tkinter maze-solver, authorized Python crawler and accessible site-auditor, safe evaluated Python AI-agent, both production Pokedex learner flows, both production feed-aggregator learner flows, both authorized Go and TypeScript crawler learner flows, the HTTP TypeScript learner studio, HTTP Servers in Go, HTTP Servers in TypeScript, HTTP Protocol in Go, RabbitMQ with TypeScript, RabbitMQ with Go, S3 and CloudFront with Go and TypeScript, and the Docker, Kubernetes, and CI/CD configuration studios without page overflow or application console errors. HTTP client Go and Python prediction flows also passed live route and grading checks. HTTP Servers in Go and TypeScript, HTTP Protocol in Go, RabbitMQ with TypeScript, RabbitMQ with Go, S3 and CloudFront with Go and TypeScript, the Python maze solver, the authorized Python crawler, the safe evaluated Python AI agent, both Pokedex tracks, both feed-aggregator tracks, and both Go and TypeScript crawler tracks each passed a full first activity from prediction through reflection, exact grading, and isolated language execution or compiler diagnostics without listener, provider, public network, database, broker, AWS, proxy, credential, native Tk, native browser, terminal, signal, host-file, or host access. Cryptography passed its first five pedagogical layers and live allowlisted AES-GCM execution without entropy, network, TLS, host, or external access. RAG, Bookbot, Asteroids, the static-site generator, the maze solver, the authorized crawler, and the safe evaluated AI agent passed cumulative pedagogical layers, exact grading, and live Python evidence. The C course passed its first six layers through exact grading and isolated Worker execution without native compiler, host, or network access. Advanced Git prediction, theory, ordering, exact evidence grading, and stateful revision simulation passed live without host or remote execution. Docker, Kubernetes, and CI/CD contract review and changed-source grading passed live while remaining disconnected from daemon, kubeconfig, cluster API, GitHub, runner, registry, OIDC, cloud, or host execution. Narrow-screen evidence records stack label and value at full width; visible controls retain 44-pixel targets, keyboard focus is visible, reduced-motion behavior is honored, and activity pages expose one logical H1 before current-step H2 content.
- Wide-screen runtime panels stay below the sticky course header while mobile and tablet panels return to document flow. Runtime actions have at least a 44-pixel target.

## Latest full verification checkpoint

After the career-launch curriculum, publication, simulator, responsive evidence-layout repair, and learner-flow verification:

- `npm test`: 75 test files and 522 tests passed under Node.js 24.18.0 and npm 12.0.1.
- `npm run type-check`: passed.
- `npm run lint`: passed across 299 source and script files.
- `npm run lint:strict`: passed across 299 files with warnings treated as failures.
- `npm run build`: passed with Next.js 16.2.10.

The Go Basics content gate parses and type-checks all 426 learner starters and lesson sources with the official Go toolchain, while the runtime tests verify local-only assets, import isolation, deterministic execution, output limits, and worker cleanup. Go research is reviewed 2026-07-14 against the Go 1.26 specification and release notes, modules reference, memory model, testing/fuzzing, race detector, context and pipelines, security, diagnostics, performance/PGO, code review guidance, and ACM/IEEE/AAAI CS2023. Current versions are Go 1.26.5 and Yaegi 0.16.1; the course explicitly distinguishes the browser subset from full-toolchain transfer gates.

The SQL content gate also executes every learner-facing SQL code block against the deterministic seed database. SQL research is reviewed 2026-07-14 against ACM/IEEE/AAAI CS2023 Data Management, SQLite 3.53.3 documentation, PostgreSQL 18.4 documentation, and OWASP SQL Injection Prevention. Browser labs are deliberately pinned to sql.js 1.14.1 with SQLite 3.49.1, and lessons identify dialect boundaries.

Run focused tests after each track, then repeat full verification. Do not infer that unverified edits after this checkpoint pass.

## Active next work

1. Research, blueprint, publish, and verify the sole remaining catalog course: current CompTIA A+. Terraform is not currently a catalog course; it appears only as a suggested next course and must not be added without an explicit scope decision. Give the A+ release strict tests for current official objective coverage, prerequisite order, scenario variety, runnable or mechanically reviewable theory, hidden-answer safety, authentic troubleshooting evidence, safety, accessibility, and cumulative transfer.
2. Audit near duplication across all activities, starters, requirements, project scenarios, assessment stems, and generated explanations—not exact duplicates alone.
3. Remove legacy routes/content only after equivalent version 2 learner flows, progress handling, and migration tests pass.
4. Complete and verify CompTIA A+, then run the full non-Lighthouse gates again before legacy migration and duplication repair.
5. Only after all planned content, migration, and duplication work is complete may the Lighthouse hold be released for mobile, tablet, and desktop scores of at least 99 in performance, accessibility, and best practices, with SEO excluded.

## Known caution

Generated quantity is not proof of instructional quality. Course completion requires source coverage, competency alignment, original explanations, varied scenarios, meaningful changed-case evidence, independent projects, accessible interaction, subject review, and learner-flow verification. Generic templates repeated across hundreds of activities are a defect even when IDs and schema checks differ. Keep catalog status coming soon until all release gates pass.

There is no external blocker recorded at this checkpoint. Tcl/Tk 9.0 packaging with PyInstaller 6.21.0, Playwright's fallback Chromium package on this host, google-adk 2.4.0's OpenTelemetry SDK 1.42.1 upper bound, and npm's transitive `whatwg-encoding` deprecation in the disposable Cheerio smoke are recorded compatibility boundaries, not blockers because the source-verified compatible smokes passed. The production build also passes with Turbopack warnings that the curriculum content-root trace matches 12,483 files and the dynamic activity path matches 11,335 files; narrowing those traces is a scalability hardening task, not a release blocker for this wave. The Lighthouse hold is intentional unfinished-work policy, not a blocker. One catalog course and the later legacy/migration/deduplication waves remain before Lighthouse may run.
