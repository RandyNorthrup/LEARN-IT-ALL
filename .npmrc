# npm 12.0.1 currently misclassifies registry-hosted bundled tarballs as
# remote URLs while expanding package-lock-only trees. Keep registry URLs
# auditable in tests while allowing that upstream path.
allow-remote=all
allow-git=none
allow-file=root
allow-directory=root
strict-allow-scripts=true
