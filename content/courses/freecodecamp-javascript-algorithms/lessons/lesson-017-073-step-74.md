---
id: lesson-017-073
title: Step 74
chapterId: chapter-17
order: 73
duration: 5
objectives:
  - Step 74
---

# Step 74

In your `highPrecedence` function, declare a variable using `const` and assign it a regex that checks if the string passed to the `str` parameter matches the pattern of a number followed by a `*` or `/` operator followed by another number.

Your function should return a boolean value. Remember that you can use the `test()` method for this.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. You should declare a variable in your `highPrecedence` function for your regex.
2. You should use `const` to declare your regex variable.
3. Your regex variable should contain a regular expression.
4. Your `highPrecedence` function should return a boolean value.
5. Your `highPrecedence` function should correctly check if the string matches the pattern of a number followed by a `*` or `/` operator followed by another number.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 646d404259f512c1a9e86ac1*
