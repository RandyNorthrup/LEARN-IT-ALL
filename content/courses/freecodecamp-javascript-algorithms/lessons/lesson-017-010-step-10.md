---
id: lesson-017-010
title: Step 10
chapterId: chapter-17
order: 10
duration: 5
objectives:
  - Step 10
---

# Step 10

Now that you have a `range` function, you can use it to create a range of letters as well.

Declare a `charRange` function using `const` and arrow syntax. It should take a `start` and `end` parameter. The function should implicitly return the result of calling `range()` with `start` and `end` as the arguments.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. You should declare a `charRange` variable.
2. Your `charRange` variable should be declared with `const`.
3. Your `charRange` variable should be a function.
4. Your `charRange` function should use arrow syntax.
5. Your `charRange` function should take `start` as the first parameter.
6. Your `charRange` function should take `end` as the second parameter.
7. Your `charRange` function should use an implicit return.
8. Your `charRange` function should call your `range` function.
9. You should pass `start` and `end` as the arguments to your `range` call.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 642e02be7845f13b014cd2b0*
