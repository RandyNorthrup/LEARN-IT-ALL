---
id: lesson-017-096
title: Step 99
chapterId: chapter-17
order: 96
duration: 5
objectives:
  - Step 99
---

# Step 99

Add a `has2` property which returns whether the `nums` array has `2` in the values, and an `increment` property which returns `nums` with every value incremented by one.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. Your `spreadsheetFunctions` object should have a `has2` property.
2. Your `has2` property should be a function.
3. Your `has2` function should return `true` if the array has `2` in it.
4. Your `has2` function should return `false` if the array does not have `2` in it.
5. Your `spreadsheetFunctions` object should have an `increment` property.
6. Your `increment` property should be a function.
7. Your `increment` function should return an array of numbers incremented by one.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 646d49bfff9079d4b38df115*
