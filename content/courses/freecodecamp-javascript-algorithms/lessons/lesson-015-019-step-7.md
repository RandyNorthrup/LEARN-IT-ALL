---
id: lesson-015-019
title: Step 7
chapterId: chapter-15
order: 19
duration: 5
objectives:
  - Step 7
---

# Step 7

Initialize another variable called `health` with a value of `100`, and a variable called `gold` with a value of `50`.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="./styles.css">
    <title>RPG - Dragon Repeller</title>
    <script src="./script.js"></script>
  </head>
  <body>
    <div id="game">
    </div>
  </body>
</html>
```

## Hints

1. You should use `let` to declare a variable called `health`.
2. You should initialize a variable called `health` with a value of `100`.
3. You should use `let` to declare a variable called `gold`.
4. You should initialize a variable called `gold` with a value of `50`.
5. `health` should have a value of `100`.
6. `gold` should have a value of `50`.
7. `xp` should still have a value of `0`.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 62a3a75d8466a12e009eff76*
