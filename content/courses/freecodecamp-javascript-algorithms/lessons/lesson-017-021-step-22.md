---
id: lesson-017-021
title: Step 22
chapterId: chapter-17
order: 21
duration: 5
objectives:
  - Step 22
---

# Step 22

Declare an `average` function which takes an array of numbers as the `nums` parameter. It should return the average of all the numbers in the array.

The average can be calculated by dividing the sum of all the numbers in the array by the length of the array. Remember that you have a `sum` function you can use.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. You should declare an `average` variable.
2. You should use `const` to declare your `average` variable.
3. Your `average` variable should be a function.
4. Your `average` function should use arrow syntax.
5. Your `average` function should have a `nums` parameter.
6. Your `average` function should use an implicit return.
7. Your `average` function should return the average value of the `nums` array.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 6437133052eaf04d7300e622*
