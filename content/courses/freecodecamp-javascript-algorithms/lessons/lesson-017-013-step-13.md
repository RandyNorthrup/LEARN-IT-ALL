---
id: lesson-017-013
title: Step 13
chapterId: chapter-17
order: 13
duration: 5
objectives:
  - Step 13
---

# Step 13

Now that your helper functions are complete, back in your `onload` event handler you should declare a `letters` variable. Assign it the result of calling `charRange()` with the letters `"A"` and `"J"` as arguments.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. You should declare a `letters` variable.
2. You should use `const` to declare your `letters` variable.
3. You should assign a `charRange()` call to your `letters` variable.
4. You should pass `"A"` as the first argument to your `charRange()` call.
5. You should pass `"J"` as the second argument to your `charRange()` call.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 64345c560591891f64976f7a*
