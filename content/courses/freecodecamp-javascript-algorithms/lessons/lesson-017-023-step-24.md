---
id: lesson-017-023
title: Step 24
chapterId: chapter-17
order: 23
duration: 5
objectives:
  - Step 24
---

# Step 24

Now declare a `length` variable and assign it the length of your sorted array, and a `middle` variable that has the value of the length divided by `2`, subtracted by `1`.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. You should declare a `length` variable after your `sorted` variable.
2. You should use `const` to declare your `length` variable.
3. You should assign the length of the `sorted` array to your `length` variable.
4. You should declare a `middle` variable after your `length` variable.
5. You should use `const` to declare your `middle` variable.
6. You should assign `middle` the value of dividing your `length` variable by `2`.
7. You should subtract `1` from your `length / 2` calculation.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 64496d1e5af8c0148fbef96d*
