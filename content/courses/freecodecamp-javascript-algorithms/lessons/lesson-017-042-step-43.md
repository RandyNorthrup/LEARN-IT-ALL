---
id: lesson-017-042
title: Step 43
chapterId: chapter-17
order: 42
duration: 5
objectives:
  - Step 43
---

# Step 43

In your `elemValue` function, declare a function called `inner` which takes a `character` parameter.

Then, return your `inner` function.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. You should declare an `inner` variable.
2. You should use `const` to declare your `inner` variable.
3. Your `inner` variable should be an arrow function.
4. Your `inner` function should have `character` as the only parameter.
5. Your `inner` function should be empty.
6. You should explicitly return your `inner` function.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 646d0889c6ff4baa46ac1c50*
