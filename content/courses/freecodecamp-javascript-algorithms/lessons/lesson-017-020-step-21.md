---
id: lesson-017-020
title: Step 21
chapterId: chapter-17
order: 20
duration: 5
objectives:
  - Step 21
---

# Step 21

Declare an `isEven` function, which takes a `num` parameter and returns `true` if the number is even, and `false` otherwise. Use the modulo operator `%` to determine if a number is even or odd.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

## Hints

1. You should declare an `isEven` variable.
2. You should use `const` to declare your `isEven` variable.
3. Your `isEven` variable should be a function.
4. Your `isEven` function should use arrow syntax.
5. Your `isEven` function should have a `num` parameter.
6. Your `isEven` function should use the modulo operator `%`.
7. Your `isEven` function should return `true` for even numbers.
8. Your `isEven` function should return `false` for odd numbers.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/javascript-algorithms-and-data-structures/)*
*Original Challenge ID: 6437124c4c03dd4c8fb35d56*
