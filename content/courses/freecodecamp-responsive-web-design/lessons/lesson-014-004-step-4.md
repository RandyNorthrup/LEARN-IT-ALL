---
id: lesson-014-004
title: Step 4
chapterId: chapter-14
order: 4
duration: 5
objectives:
  - Step 4
---

# Step 4

Add a `title` and `meta` element inside the `head` element. Give your project a title of `Registration Form`, and add the `charset` attribute with a value of `utf-8` to your `meta` element.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  --fcc-editable-region--
  <head>
    
  </head>
--fcc-editable-region--
  <body>
  </body>
</html>
```

## Hints

1. Your code should have a `title` element.
2. Your `title` element should be within the `head` element.
3. Your project should have a title of `Registration Form`. Remember, the casing and spelling matters for the title.
4. You should create a `meta` element within the `head` element.
5. You should give the `meta` element a `charset` attribute with the value of `utf-8`.
6. Your `meta` element should be a void element, it does not have an end tag `</meta>`.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/responsive-web-design/)*
*Original Challenge ID: 60f0286404aefb0562a4fdf9*
