---
id: lesson-014-009
title: Step 10
chapterId: chapter-14
order: 9
duration: 5
objectives:
  - Step 10
---

# Step 10

That is better. Now, make the background easy on the eyes, by changing the `body` `background-color` to `#1b1b32`. Then, to see the text, change the `color` to `#f5f6f7`.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Registration Form</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <h1>Registration Form</h1>
    <p>Please fill out this form with the required information</p>
  </body>
</html>
```

## Hints

1. You should add the `background-color` within the `body` element selector.
2. You should give the `background-color` a value of `#1b1b32`.
3. You should add the `color` within the `body` element selector.
4. You should give the `color` a value of `#f5f6f7`.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/responsive-web-design/)*
*Original Challenge ID: 60f1a9cbd23023082e149fee*
