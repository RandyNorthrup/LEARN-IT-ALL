---
id: lesson-012-031
title: Step 31
chapterId: chapter-12
order: 31
duration: 5
objectives:
  - Step 31
---

# Step 31

Give the six new elements these `width` and `height` values: `10%` and `60%` to `.fb1`, `10%` and `40%` to `.fb2`, `10%` and `35%` to `.fb3`, `8%` and `45%` to `.fb4`, `10%` and `33%` to `.fb5`, and `9%` and `38%` to `.fb6`.

## Starter Code

```html
<!DOCTYPE html>
<html lang="en">    
  <head>
    <meta charset="UTF-8">
    <title>City Skyline</title>
    <link href="styles.css" rel="stylesheet" />   
  </head>

  <body>
    <div class="background-buildings">
      <div></div>
      <div></div>
      <div class="bb1">
        <div class="bb1a"></div>
        <div class="bb1b"></div>
        <div class="bb1c"></div>
        <div class="bb1d"></div>
      </div>
      <div class="bb2"></div>
      <div class="bb3"></div>
      <div></div>
      <div class="bb4"></div>
      <div></div>
      <div></div>
    </div>

    <div class="foreground-buildings">
      <div class="fb1"></div>
      <div class="fb2"></div>
      <div class="fb3"></div>
      <div class="fb4"></div>
      <div class="fb5"></div>
      <div class="fb6"></div>
    </div>
  </body>
</html>
```

## Hints

1. You should create a `.fb1` selector.
2. You should give the `.fb1` selector a `width` of `10%`.
3. You should give the `.fb1` selector a `height` of `60%`.
4. You should create a `.fb2` selector.
5. You should give the `.fb2` selector a `width` of `10%`.
6. You should give the `.fb2` selector a `height` of `40%`.
7. You should create a `.fb3` selector.
8. You should give the `.fb3` selector a `width` of `10%`.
9. You should give the `.fb3` selector a `height` of `35%`.
10. You should create a `.fb4` selector.
11. You should give the `.fb4` selector a `width` of `8%`.
12. You should give the `.fb4` selector a `height` of `45%`.
13. You should create a `.fb5` selector.
14. You should give the `.fb5` selector a `width` of `10%`.
15. You should give the `.fb5` selector a `height` of `33%`.
16. You should create a `.fb6` selector.
17. You should give the `.fb6` selector a `width` of `9%`.
18. You should give the `.fb6` selector a `height` of `38%`.

---

*Source: [freeCodeCamp](https://www.freecodecamp.org/learn/responsive-web-design/)*
*Original Challenge ID: 5d822fd413a79914d39e98e7*
