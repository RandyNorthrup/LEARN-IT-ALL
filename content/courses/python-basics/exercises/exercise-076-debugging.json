{
  "id": "exercise-076-debugging",
  "lessonId": "lesson-064-debugging-techniques",
  "title": "Debugging Practice",
  "instructions": "Find and fix the bugs in the provided functions. Each function has a logical error that needs to be corrected.",
  "difficulty": "intermediate",
  "starterCode": "def calculate_average(numbers):\n    \"\"\"Calculate average of numbers - HAS BUG\"\"\"\n    total = 0\n    for num in numbers:\n        total + num\n    return total / len(numbers)\n\ndef remove_duplicates(items):\n    \"\"\"Remove duplicates preserving order - HAS BUG\"\"\"\n    for item in items:\n        if items.count(item) > 1:\n            items.remove(item)\n    return items\n\ndef is_palindrome(text):\n    \"\"\"Check if text is palindrome - HAS BUG\"\"\"\n    return text == text[::-1]",
  "solution": "def calculate_average(numbers):\n    \"\"\"Calculate average of numbers\"\"\"\n    total = 0\n    for num in numbers:\n        total += num  # Fixed: added = to make it +=\n    return total / len(numbers)\n\ndef remove_duplicates(items):\n    \"\"\"Remove duplicates preserving order\"\"\"\n    seen = set()\n    result = []\n    for item in items:\n        if item not in seen:\n            seen.add(item)\n            result.append(item)\n    return result  # Fixed: create new list instead of modifying while iterating\n\ndef is_palindrome(text):\n    \"\"\"Check if text is palindrome\"\"\"\n    text = text.lower().replace(\" \", \"\")  # Fixed: normalize text\n    return text == text[::-1]",
  "hints": [
    "calculate_average: Check if the total is actually being updated in the loop",
    "remove_duplicates: Modifying a list while iterating causes issues. Try creating a new list.",
    "is_palindrome: Consider case sensitivity and spaces. 'Race Car' should be a palindrome."
  ],
  "language": "python",
  "testCases": [],
  "description": "Find and fix the bugs in the provided functions. Each function has a logical error that needs to be corrected.",
  "points": 10
}