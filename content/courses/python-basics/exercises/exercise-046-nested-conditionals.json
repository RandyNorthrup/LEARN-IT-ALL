{
  "id": "exercise-046-nested-conditionals",
  "lessonId": "lesson-030-nested-conditionals",
  "title": "Nested Conditionals and Ternary Practice",
  "instructions": "Practice nested conditionals and ternary operators. Write clean, readable conditional logic.",
  "difficulty": "intermediate",
  "starterCode": "def get_ticket_price(age, is_student):\n    \"\"\"Calculate ticket price based on age and student status\n    Base: $20, Children (under 12): $10, Seniors (65+): $15, Students: $12\"\"\"\n    pass\n\ndef check_login(username, password, is_active):\n    \"\"\"Check login credentials\n    Return 'success', 'inactive', 'invalid_username', or 'invalid_password'\"\"\"\n    pass\n\ndef get_shipping_cost(weight, is_express):\n    \"\"\"Calculate shipping: standard $5 (<=5kg), $10 (<=20kg), $15 (>20kg)\n    Express adds 50% to cost\"\"\"\n    pass",
  "solution": "def get_ticket_price(age, is_student):\n    \"\"\"Calculate ticket price based on age and student status\n    Base: $20, Children (under 12): $10, Seniors (65+): $15, Students: $12\"\"\"\n    if age < 12:\n        return 10\n    elif age >= 65:\n        return 15\n    elif is_student:\n        return 12\n    else:\n        return 20\n\ndef check_login(username, password, is_active):\n    \"\"\"Check login credentials\n    Return 'success', 'inactive', 'invalid_username', or 'invalid_password'\"\"\"\n    if username != \"admin\":\n        return \"invalid_username\"\n    \n    if password != \"secret\":\n        return \"invalid_password\"\n    \n    if not is_active:\n        return \"inactive\"\n    \n    return \"success\"\n\ndef get_shipping_cost(weight, is_express):\n    \"\"\"Calculate shipping: standard $5 (<=5kg), $10 (<=20kg), $15 (>20kg)\n    Express adds 50% to cost\"\"\"\n    if weight <= 5:\n        base_cost = 5\n    elif weight <= 20:\n        base_cost = 10\n    else:\n        base_cost = 15\n    \n    return base_cost * 1.5 if is_express else base_cost",
  "hints": [
    "Use if-elif-else for multiple conditions",
    "Check conditions in order from most specific to most general",
    "Use ternary operator for simple two-way choices",
    "Guard clauses help flatten nested logic"
  ],
  "language": "python",
  "testCases": [],
  "description": "Practice nested conditionals and ternary operators. Write clean, readable conditional logic.",
  "points": 10
}