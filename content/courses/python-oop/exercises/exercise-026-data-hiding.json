{
  "id": "exercise-026-data-hiding",
  "courseId": "python-oop",
  "lessonId": "lesson-026-data-hiding",
  "title": "Hide Implementation Details",
  "description": "Create a BankAccount with private balance",
  "difficulty": "intermediate",
  "points": 25,
  "language": "python",
  "starterCode": "# Create BankAccount with private __balance\n\nclass BankAccount:\n    def __init__(self, initial_balance):\n        self.__balance = initial_balance\n    \n    def deposit(self, amount):\n        self.__balance += amount\n    \n    def get_balance(self):\n        return self.__balance\n\naccount = BankAccount(100)\naccount.deposit(50)\nprint(account.get_balance())",
  "solution": "class BankAccount:\n    def __init__(self, initial_balance):\n        self.__balance = initial_balance\n    \n    def deposit(self, amount):\n        if amount > 0:\n            self.__balance += amount\n    \n    def get_balance(self):\n        return self.__balance\n\naccount = BankAccount(100)\naccount.deposit(50)\nprint(account.get_balance())",
  "hints": [
    "Use __ prefix for private attributes",
    "Provide public methods to interact with private data",
    "Validate deposits (must be positive)",
    "Cannot access __balance directly from outside"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should manage private balance",
      "input": "",
      "expectedOutput": "150",
      "isHidden": false
    }
  ]
}
