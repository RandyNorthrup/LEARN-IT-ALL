{
  "id": "exercise-031-encapsulation-benefits",
  "courseId": "python-oop",
  "lessonId": "lesson-031-encapsulation-benefits",
  "title": "Benefits of Encapsulation",
  "description": "Refactor code to use proper encapsulation",
  "difficulty": "intermediate",
  "points": 25,
  "language": "python",
  "starterCode": "# Refactor to use encapsulation properly\n\nclass User:\n    def __init__(self, username, password):\n        self.username = username\n        self.password = password  # Bad: password exposed!\n\nuser = User(\"alice\", \"secret123\")\nprint(f\"Username: {user.username}\")\n# Don't print password!",
  "solution": "class User:\n    def __init__(self, username, password):\n        self.username = username\n        self.__password = password  # Private!\n    \n    def check_password(self, password):\n        return self.__password == password\n\nuser = User(\"alice\", \"secret123\")\nprint(f\"Username: {user.username}\")\nprint(f\"Password correct: {user.check_password('secret123')}\")",
  "hints": [
    "Make password private with __",
    "Don't expose password directly",
    "Provide check_password() method instead",
    "This is more secure"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should check password securely",
      "input": "",
      "expectedOutput": "Username: alice\nPassword correct: True",
      "isHidden": false
    }
  ]
}
