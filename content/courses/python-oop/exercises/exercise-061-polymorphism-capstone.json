{
  "id": "exercise-061-polymorphism-capstone",
  "courseId": "python-oop",
  "lessonId": "lesson-061-polymorphism-capstone",
  "title": "Build Notification System",
  "description": "Create complete multi-channel notification system",
  "difficulty": "advanced",
  "points": 50,
  "language": "python",
  "starterCode": "from abc import ABC, abstractmethod\n\nclass NotificationChannel(ABC):\n    @abstractmethod\n    def send(self, message):\n        pass\n\n# Create Email, SMS, and Push channels\n\nclass NotificationManager:\n    def __init__(self):\n        self.channels = []\n    \n    def add_channel(self, channel):\n        self.channels.append(channel)\n    \n    def notify_all(self, message):\n        for channel in self.channels:\n            channel.send(message)\n\nmanager = NotificationManager()\nmanager.add_channel(Email())\nmanager.add_channel(SMS())\nmanager.add_channel(Push())\nmanager.notify_all(\"System alert!\")",
  "solution": "from abc import ABC, abstractmethod\n\nclass NotificationChannel(ABC):\n    @abstractmethod\n    def send(self, message):\n        pass\n\nclass Email(NotificationChannel):\n    def send(self, message):\n        print(f\"Email: {message}\")\n\nclass SMS(NotificationChannel):\n    def send(self, message):\n        print(f\"SMS: {message}\")\n\nclass Push(NotificationChannel):\n    def send(self, message):\n        print(f\"Push: {message}\")\n\nclass NotificationManager:\n    def __init__(self):\n        self.channels = []\n    \n    def add_channel(self, channel):\n        self.channels.append(channel)\n    \n    def notify_all(self, message):\n        for channel in self.channels:\n            channel.send(message)\n\nmanager = NotificationManager()\nmanager.add_channel(Email())\nmanager.add_channel(SMS())\nmanager.add_channel(Push())\nmanager.notify_all(\"System alert!\")",
  "hints": [
    "Create Email, SMS, Push classes",
    "All implement send() method",
    "Manager works with any channel",
    "Demonstrates complete polymorphism"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should send via all channels",
      "input": "",
      "expectedOutput": "Email: System alert!\nSMS: System alert!\nPush: System alert!",
      "isHidden": false
    }
  ]
}
