{
  "id": "exercise-008-documentation",
  "courseId": "python-oop",
  "lessonId": "lesson-008-documentation",
  "title": "Add Proper Documentation",
  "description": "Add docstrings and comments to document the code",
  "difficulty": "beginner",
  "points": 15,
  "language": "python",
  "starterCode": "def calculate(numbers, operation):\n    if operation == 'sum':\n        return sum(numbers)\n    elif operation == 'avg':\n        return sum(numbers) / len(numbers)\n    return 0\n\nprint(calculate([1, 2, 3, 4, 5], 'sum'))\nprint(calculate([10, 20, 30], 'avg'))",
  "solution": "def calculate(numbers, operation):\n    \"\"\"Perform calculations on a list of numbers.\n    \n    Args:\n        numbers: List of numbers to calculate\n        operation: Type of operation ('sum' or 'avg')\n    \n    Returns:\n        float: Result of the calculation\n    \"\"\"\n    if operation == 'sum':\n        return sum(numbers)\n    elif operation == 'avg':\n        return sum(numbers) / len(numbers)\n    return 0\n\nprint(calculate([1, 2, 3, 4, 5], 'sum'))\nprint(calculate([10, 20, 30], 'avg'))",
  "hints": [
    "Add a docstring with triple quotes",
    "Document parameters using Args section",
    "Document return value using Returns section",
    "Follow Google or NumPy docstring style"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should calculate sum and average",
      "input": "",
      "expectedOutput": "15\n20.0",
      "isHidden": false
    }
  ]
}
