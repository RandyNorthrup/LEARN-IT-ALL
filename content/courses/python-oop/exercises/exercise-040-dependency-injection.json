{
  "id": "exercise-040-dependency-injection",
  "courseId": "python-oop",
  "lessonId": "lesson-040-dependency-injection",
  "title": "Use Dependency Injection",
  "description": "Inject dependencies through abstraction",
  "difficulty": "advanced",
  "points": 35,
  "language": "python",
  "starterCode": "from abc import ABC, abstractmethod\n\nclass Logger(ABC):\n    @abstractmethod\n    def log(self, message):\n        pass\n\nclass FileLogger(Logger):\n    def log(self, message):\n        print(f\"File: {message}\")\n\n# Create UserService that depends on Logger\nclass UserService:\n    def __init__(self, logger):\n        self.logger = logger\n    \n    def create_user(self, name):\n        self.logger.log(f\"Creating user: {name}\")\n\nlogger = FileLogger()\nservice = UserService(logger)\nservice.create_user(\"Alice\")",
  "solution": "from abc import ABC, abstractmethod\n\nclass Logger(ABC):\n    @abstractmethod\n    def log(self, message):\n        pass\n\nclass FileLogger(Logger):\n    def log(self, message):\n        print(f\"File: {message}\")\n\nclass UserService:\n    def __init__(self, logger):\n        self.logger = logger\n    \n    def create_user(self, name):\n        self.logger.log(f\"Creating user: {name}\")\n\nlogger = FileLogger()\nservice = UserService(logger)\nservice.create_user(\"Alice\")",
  "hints": [
    "UserService depends on Logger abstraction",
    "Inject logger in __init__",
    "Service works with any Logger implementation",
    "This is dependency injection"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should inject and use logger",
      "input": "",
      "expectedOutput": "File: Creating user: Alice",
      "isHidden": false
    }
  ]
}
