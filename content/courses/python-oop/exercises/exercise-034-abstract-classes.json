{
  "id": "exercise-034-abstract-classes",
  "courseId": "python-oop",
  "lessonId": "lesson-034-abstract-classes",
  "title": "Create Abstract Class",
  "description": "Define an abstract Shape class",
  "difficulty": "intermediate",
  "points": 20,
  "language": "python",
  "starterCode": "from abc import ABC, abstractmethod\n\n# Create abstract Shape with area() method\n\n# Create Circle that implements area()\n\ncircle = Circle(5)\nprint(circle.area())",
  "solution": "from abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self):\n        pass\n\nclass Circle(Shape):\n    def __init__(self, radius):\n        self.radius = radius\n    \n    def area(self):\n        return 3.14159 * self.radius ** 2\n\ncircle = Circle(5)\nprint(circle.area())",
  "hints": [
    "Create Shape(ABC) abstract class",
    "Add @abstractmethod area()",
    "Circle must implement area()",
    "Area of circle = π * r²"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should calculate circle area",
      "input": "",
      "expectedOutput": "78.53975",
      "isHidden": false
    }
  ]
}
