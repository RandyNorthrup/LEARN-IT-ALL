{
  "id": "exercise-056-abstract-base-classes",
  "courseId": "python-oop",
  "lessonId": "lesson-056-abstract-base-classes",
  "title": "Use ABCs with Polymorphism",
  "description": "Create payment system with ABC",
  "difficulty": "advanced",
  "points": 35,
  "language": "python",
  "starterCode": "from abc import ABC, abstractmethod\n\nclass PaymentMethod(ABC):\n    @abstractmethod\n    def process(self, amount):\n        pass\n\n# Create CreditCard and PayPal implementations\n\ndef process_payment(payment_method, amount):\n    payment_method.process(amount)\n\ncc = CreditCard()\npaypal = PayPal()\nprocess_payment(cc, 100)\nprocess_payment(paypal, 50)",
  "solution": "from abc import ABC, abstractmethod\n\nclass PaymentMethod(ABC):\n    @abstractmethod\n    def process(self, amount):\n        pass\n\nclass CreditCard(PaymentMethod):\n    def process(self, amount):\n        print(f\"Processing ${amount} via credit card\")\n\nclass PayPal(PaymentMethod):\n    def process(self, amount):\n        print(f\"Processing ${amount} via PayPal\")\n\ndef process_payment(payment_method, amount):\n    payment_method.process(amount)\n\ncc = CreditCard()\npaypal = PayPal()\nprocess_payment(cc, 100)\nprocess_payment(paypal, 50)",
  "hints": [
    "Both implement process()",
    "ABC enforces the contract",
    "process_payment works with any PaymentMethod",
    "Polymorphism with ABC"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should process payments",
      "input": "",
      "expectedOutput": "Processing $100 via credit card\nProcessing $50 via PayPal",
      "isHidden": false
    }
  ]
}
