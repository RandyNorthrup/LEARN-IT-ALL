{
  "id": "exercise-029-immutability",
  "courseId": "python-oop",
  "lessonId": "lesson-029-immutability",
  "title": "Create Immutable Objects",
  "description": "Create an immutable Point class",
  "difficulty": "intermediate",
  "points": 25,
  "language": "python",
  "starterCode": "# Create immutable Point class (x, y cannot be changed after creation)\n\nclass Point:\n    def __init__(self, x, y):\n        # Make x and y read-only\n        pass\n\npoint = Point(5, 10)\nprint(f\"({point.x}, {point.y})\")\n# point.x = 20  # Should not work",
  "solution": "class Point:\n    def __init__(self, x, y):\n        self._x = x\n        self._y = y\n    \n    @property\n    def x(self):\n        return self._x\n    \n    @property\n    def y(self):\n        return self._y\n\npoint = Point(5, 10)\nprint(f\"({point.x}, {point.y})\")",
  "hints": [
    "Store values in private attributes",
    "Provide only getters (no setters)",
    "Use @property for read-only access",
    "No setter means immutable"
  ],
  "testCases": [
    {
      "id": "tc1",
      "description": "Should create immutable point",
      "input": "",
      "expectedOutput": "(5, 10)",
      "isHidden": false
    }
  ]
}
